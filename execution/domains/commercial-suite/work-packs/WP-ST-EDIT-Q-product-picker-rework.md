# WORK PACKAGE — WP-ST-EDIT-Q · Ürün bölümü mockup rework: aramalı "Ürün ekle" picker (buton yok) + sayfalı load (400 fix) + boş-durum + progress bar (frontend)

> **CT (SoR).** MOD-0167-FU04. Branch `feature/crm-scmm-studio` (`956e40b2` üstü — WP-ST-EDIT-P §37 sonrası). **Kök neden (canlı doğrulandı):** Ürün/SKU picker'ları boş çünkü form.js `?pageSize=200` gönderiyor ama MDM selector validator'ı `PageSize`'ı **1–100** ile sınırlıyor → **400 Bad Request** → `load()` yakalayıp picker'ı boş bırakıyor. (RBAC `mdm.global-products.read` verilmiş, re-login ile token'a geçti; `can('global-product')` artık true.) **Kullanıcı kararı:** "Tam mockup rework". **Frontend** (form.js + _Form.cshtml + _IndexL10n.cshtml + strategy-create.css + 7 resx). Backend/contract/**MDM validator DEĞİŞMEZ** (frontend cap=100'e uyar, validator gevşetilmez).

## Kanıt
- `services/Diten.MdmService/.../Validators/GetGlobalProductSelectorValidator.cs:11` + `GetFinishedGoodGskuSelectorValidator.cs`: `RuleFor(x => x.PageSize).InclusiveBetween(1, 100)`. → `pageSize=200` reddedilir → ValidationBehavior 400.
- `form.js` `load()` (107-117): `envelope()` `body.data` döner; `data.items` map+filter(o=>o.id); hata→`[]` (picker boş, uydurmaz). `data.totalCount` mevcut (PagedResult.TotalCount → camelCase).
- `form.js` loadOptions (153-168): **yalnız** `global-products` (154) ve `gskus` (164) `?pageSize=200` gönderir; segment/VFP/path/journey pageSize göndermez (o yüzden çalışırlar). MDM'de 177 global product var → cap=100 tek sayfada hepsini getirmez.
- `form.js` renderProducts (476-500): satır = `pickerSelect('product', line.globalProductId, ...)` (düzenlenebilir dropdown) + `st-prod-meta` MDM-GP kod + `js-weight` % + Kaldır ikon. `productById` (441) id→{text,code,name}. `#productLineEmpty` (478/498) düz text-muted (dashed kutu değil). Σ badge/Eşit dağıt/uyarı = EDIT-P (updateProductTotals 446).
- `_Form.cshtml` productSection (244-272): `#btnAddProductLine` (248) buton; `TotalMustBe100` (250, SKU metni); `.st-prod-controls` Σ badge + Eşit dağıt (259-265); `#productWeightWarn` (267); `#productLineList` (268); `#productLineEmpty` (269 `NoProductLines`).
- `form.js` addProduct (591-598): `#btnAddProductLine` click → `state.products.push({...})` + renderProducts. bindSelect2(select,{search}) helper (EDIT-O) mevcut (search:true aramalı). Segment picker deseni (renderSegmentPicker 251-272 + `.st-segment-choice` + `.st-segment-empty` dashed css:400) referans.
- Mockup (/c/tmp/mockup-strategy.html Düzenle Ürün): başlık "Ürün + SKU% — ne"; helper "Ürünler MDM Global Product kataloğundan doğrulanır."; boş-durum "Ürün satırı yok — oyunun hangi ürünleri taşıdığını ekleyin."; progress bar; **satırda ürün AD (sabit) + MDM-GP kod**, ayrı "ürün hattı ekle" butonu YOK (ekleme aramalı picker ile).
- `_IndexL10n.cshtml` (17-33): form.js `L` köprüsü whitelist — form.js'in okuduğu YENİ anahtar buraya eklenmeli.

## NE (frontend; backend/contract/MDM validator DEĞİŞMEZ)
1. **Sayfalı load (400 fix + tüm 177 ürün):** `load()`'a sayfalama ekle (yeni yardımcı `loadAll(url, map)` ya da `load` içine pageSize döngüsü): `pageSize=100` ile 1. sayfayı çek, envelope `data.totalCount`'a göre kalan sayfaları (`pageNumber=2..`) çek, tüm `items` birleştir, map+filter. **`?pageSize=200` KALDIR** — global-products ve gskus için `pageSize=100` sayfalı. Segment/VFP/path/journey load'ları DEĞİŞMEZ. Mapping (id/canonicalCode/globalProductName) DEĞİŞMEZ. Güvenlik: max sayfa sınırı (ör. 50 sayfa) sonsuz döngüye karşı.
2. **Ürün ekleme = aramalı picker (buton YOK):** `#btnAddProductLine` KALDIR (_Form + form.js addProduct handler). Bölüm başına **aramalı single select2 "Ürün ekle…"** (`#productAddPicker`; bindSelect2 search:true; `dropdownParent`; jQuery/select2 yoksa düz select degrade). Opsiyonlar = seçili olmayan `options.product` (segment gibi `chosen` filtresi — zaten eklenen ürün tekrar önerilmez). Seçim → `state.products.push({ globalProductId, lineWeightPercentage:'', skuAllocationMode:'product-only', skuAllocations:[] })` + picker sıfırla (value '') + renderProducts. `!can('global-product')` → picker disabled + `PickerUnavailable` (pickerSelect deseni). `maxProductLines`'a ulaşınca picker disabled + `LimitReached`.
3. **Satırda ürün SABİT etiket (mockup):** renderProducts satır ürününü **salt-okunur etikete** çevir: ürün **adı** (fw) + alt-satır **MDM-GP kod** (`st-prod-meta`) — düzenlenebilir `pickerSelect` DEĞİL (mockup satırda ürünü sabit gösterir; değiştirmek = Kaldır + yeniden ekle). `%` input (`js-weight`) + Kaldır ikon KORUNUR. `productById` çözünürse ad/kod, çözmezse ham globalProductId göster (veri kaybı yok).
4. **Boş-durum dashed kutu:** `#productLineEmpty` → `.st-segment-empty` gibi **dashed bg-body kutu** (yeni `.st-prod-empty` veya `.st-segment-empty` sınıfı reuse), metin **"Ürün satırı yok — oyunun hangi ürünleri taşıdığını ekleyin."** (resx `NoProductLines` bu metne güncelle, 7 dil).
5. **Helper + başlık:** başlık altında **"Ürünler MDM Global Product kataloğundan doğrulanır."** (yeni resx `ProductCatalogHelp`, 7 dil, Razor `text-muted`). `TotalMustBe100` metnini **flat ürün-yüzdesi** diline güncelle (SKU değil: "Tüm ürün satırlarının toplamı tam %100 olmalı…", 7 dil). Başlık `ProductSection` mockup "Ürün + SKU% — Ne" (mevcut değeri koru/uydur; text-uppercase).
6. **Progress bar:** Σ badge yanında görsel bar (0–100 dolum; ≠100 → danger renk, =100 → success). `updateProductTotals()` bar genişliğini `min(total,100)%` + renk sınıfını günceller. Markup `_Form` (`#productWeightBar`) + css (`.st-prod-progress`). round2.
7. **css + resx (7 dil) + L köprüsü:** dashed boş-hal + progress bar + aramalı picker layout; yeni/değişen resx (`NoProductLines`, `TotalMustBe100`, `ProductCatalogHelp`, gerekiyorsa picker placeholder); **form.js'in okuduğu YENİ anahtarları `_IndexL10n` whitelist'ine ekle**. PascalCase köprü.

## KORU / YAPMA
- Backend/contract/**MDM validator DEĞİŞMEZ** — MDM `PageSize` cap=100 **gevşetilmez**; frontend sayfalı çekerek uyar. ProductLinesJson (globalProductId/lineWeightPercentage/skuAllocationMode/skuAllocations) sözleşmesi KORUNUR — UI product-only yazar; gelen sku-allocated satır datası **WIPE EDİLMEZ** (sync yalnız globalProductId+lineWeightPercentage okur, EDIT-P). Σ badge/Eşit dağıt/`#productWeightWarn` (EDIT-P) davranışı KORUNUR. renderProducts dışı (Segment/Frekans/İçerik/KAPSAM/sağ panel) DOKUNMA. Liste/Detay/details.js/CrmService/Controller/ViewModel DOKUNMA. Ürün load mapping (id/canonicalCode/globalProductName) KORUNUR. Uydurma % / uydurma ürün yok. Tema/L10n köprüsü.
- **DUR:** envelope `totalCount` yok/güvenilmezse (sayfalama kurulamaz) → DUR+raporla (geçici pageSize=100 tek sayfa DEĞİL, raporla). Satır ürün etiketi globalProductId çözünürlüğünü/round-trip'i bozuyorsa; sabit etikete geçiş sync sözleşmesini değiştiriyorsa → DUR+raporla.

## Acceptance
- **E2:** Diten.Web.Tests 201/0. git diff: form.js + _Form.cshtml + _IndexL10n.cshtml + strategy-create.css + 7 resx. **Backend/Controller/ViewModel/StrategyTemplateModels/MDM/liste/detay diff YOK.**
- **E4 (re-login sonrası):** Ürün bölümünde **ürünler gelir** (aramalı "Ürün ekle…" ile 177'nin tamamına arama; 400 yok — Network temiz); ayrı ekle butonu YOK; satır = ürün adı + MDM-GP kod + % + Kaldır ikon; boş-durum dashed kutu mockup metniyle; progress bar Σ'yi gösterir (≠100 kırmızı); Σ badge + Eşit dağıt + uyarı (EDIT-P) çalışır; kaydet→ProductLinesJson product-only doğru + gelen sku-allocated satır korunur. **Razor+css+resx → FLEET RESTART; js → Ctrl+F5.**

---

## §36.1 Agent Prompt (paste-ready) — DISPATCH: owner
```text
@[.antigravity/agents/frontend-specialist.md]
WP: WP-ST-EDIT-Q · Ürün bölümü mockup rework — aramalı "Ürün ekle" picker (buton yok) + sayfalı load (400 fix) + dashed boş-durum + progress bar (MOD-0167-FU04, frontend)
Repository: C:\Users\user\Desktop\ERP-vNext (ANA checkout)
Branch: feature/crm-scmm-studio · Expected HEAD: 956e40b2 üstü · Worktree: ana checkout

KÖK NEDEN (canlı doğrulandı): form.js `?pageSize=200` → MDM selector validator PageSize 1–100 → 400 → picker boş. MDM validator DEĞİŞMEZ; frontend sayfalı çekerek uyar.

Önce oku: execution/domains/commercial-suite/work-packs/WP-ST-EDIT-Q-product-picker-rework.md · frontend/Diten.Web/wwwroot/assets/js/CRM/StrategyTemplates/form.js (load 107-117 + envelope 101-105, loadOptions 153-168 global-products/gskus pageSize=200, renderProducts 476-500, updateProductTotals 446-470, productById 441, addProduct handler ~591-598, bindSelect2/unbindSelect2 helper EDIT-O, renderSegmentPicker 251-272 + chosen filtresi referans) · frontend/Diten.Web/Views/CRM/StrategyTemplates/_Form.cshtml (productSection 244-272) · _IndexL10n.cshtml (L köprüsü whitelist 17-33) · wwwroot/assets/css/strategy-create.css (.st-prod-* 510-561, .st-segment-empty 400 dashed referans) · 7 resx (Resources/Views/CRM/StrategyTemplates/StrategyTemplatesIndex.<lang>.resx). Mockup: /c/tmp/mockup-strategy.html Düzenle Ürün (server 127.0.0.1:8799).

NE (frontend; backend/contract/MDM validator DEĞİŞMEZ):
 1) Sayfalı load: load()'a pageSize=100 sayfalama (envelope data.totalCount'a göre kalan sayfaları çek, items birleştir, map+filter; max ~50 sayfa güvenlik). global-products + gskus `?pageSize=200` KALDIR → pageSize=100 sayfalı. Segment/VFP/path/journey load DEĞİŞMEZ. Mapping (id/canonicalCode/globalProductName) DEĞİŞMEZ.
 2) Ürün ekleme = aramalı picker, buton YOK: #btnAddProductLine KALDIR (_Form + form.js). Bölüm başı aramalı single select2 #productAddPicker (bindSelect2 search:true; jQuery yoksa düz select degrade); opsiyon = seçilmemiş options.product (chosen filtresi); seçim → state.products.push({globalProductId, lineWeightPercentage:'', skuAllocationMode:'product-only', skuAllocations:[]}) + picker reset + renderProducts. !can('global-product') → disabled+PickerUnavailable; maxProductLines dolunca disabled+LimitReached.
 3) Satırda ürün SABİT etiket: renderProducts satır ürününü salt-okunur ad (fw) + alt MDM-GP kod (st-prod-meta) yap (pickerSelect DEĞİL); % input js-weight + Kaldır ikon KORU; productById çözmezse ham globalProductId göster.
 4) Boş-durum dashed: #productLineEmpty → .st-segment-empty gibi dashed bg-body kutu; NoProductLines = "Ürün satırı yok — oyunun hangi ürünleri taşıdığını ekleyin." (7 dil).
 5) Helper+başlık: başlık altı "Ürünler MDM Global Product kataloğundan doğrulanır." (yeni ProductCatalogHelp, 7 dil, Razor text-muted); TotalMustBe100 flat ürün-yüzdesi diline güncelle (7 dil); ProductSection "Ürün + SKU% — Ne".
 6) Progress bar: Σ badge yanı görsel bar (#productWeightBar; genişlik min(total,100)%, ≠100 danger / =100 success); updateProductTotals bar+renk günceller; markup _Form + css .st-prod-progress.
 7) css + resx 7 dil + L köprüsü: dashed boş-hal + progress + aramalı picker; form.js'in okuduğu YENİ anahtarları _IndexL10n whitelist'ine ekle; PascalCase köprü.
KORU/YAPMA: backend/contract/MDM validator DEĞİŞMEZ (cap=100 gevşetilmez); ProductLinesJson sözleşmesi korunur (UI product-only yazar, gelen sku-allocated datası WIPE EDİLMEZ; sync yalnız globalProductId+lineWeightPercentage okur); Σ/Eşit dağıt/#productWeightWarn (EDIT-P) korunur; Segment/Frekans/İçerik/KAPSAM/sağ panel DOKUNMA; Liste/Detay/details.js/CrmService/Controller/ViewModel DOKUNMA; ürün load mapping korunur; uydurma yok; tema/L10n köprüsü.
DOĞRULA (E2): cd C:\Users\user\Desktop\ERP-vNext; dotnet test frontend/Diten.Web.Tests/Diten.Web.Tests.csproj -c Release --nologo → 201/0; git diff form.js+_Form+_IndexL10n+css+7 resx; backend/Controller/ViewModel/MDM/liste/detay diff yok. Ayrı commit ("feat(strategy): WP-ST-EDIT-Q — Ürün bölümü mockup rework: aramalı 'Ürün ekle' picker + sayfalı load (400 fix) + boş-durum + progress bar (MOD-0167-FU04)" + son satır Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>). §22 TÜRKÇE. K13.
Durma: envelope totalCount yok/güvenilmezse; sabit ürün etiketi round-trip/sync sözleşmesini bozuyorsa → DUR+raporla.
```

## §37 CT bağımsız doğrulama (2026-09-21) → **ACCEPTED (E2)**
```
Commit: 5d382c57 · Agent: PASS · CT: ACCEPTED E2 (izole temiz build) · /c/tmp/ct-steitq-verify @5d382c57
```
- ✅ **Kapsam (11 dosya, +206/−43):** form.js + _Form.cshtml + _IndexL10n.cshtml + strategy-create.css + 7 resx. **backend/Controller/ViewModel/StrategyTemplateModels/MDM/liste/detay TEMİZ** ✓.
- ✅ **400 fix (loadAll):** `loadAll(baseUrl, map)` (128) pageSize=100 sayfalı — 1. sayfa + `data.totalCount`'a göre kalan sayfalar (pageNumber=2..), `PICKER_MAX_PAGES` tavanı; global-products (189) + gskus (200) `loadAll` (pageSize=200 kaldırıldı). Segment/VFP/path/journey `load()` dokunulmadı, mapping korundu.
- ✅ **Aramalı picker + sabit satır:** `#btnAddProductLine` kalktı; `#productAddPicker` aramalı select2 (chosen filtresi 537); seçim → product-only satır push + reset (0ms ertelemeli, select2 teardown çakışması önlemi). Satır ürünü salt-okunur ad + MDM-GP kod (pickerSelect değil); `productById` çözmezse ham globalProductId (556).
- ✅ **KORU=0 (ProductLinesJson):** `ProductLinesJson = JSON.stringify(state.products)` (817) → skuAllocationMode/skuAllocations/globalProductId state'te byte-for-byte korunur; yalnız lineWeightPercentage input'tan yazılır (769); ürün id DOM'dan yeniden OKUNMAZ (sabit etiket → veri kaybı yok). Eşit dağıt yalnız lineWeightPercentage set eder (skuAllocations'a dokunmaz). Σ/Eşit dağıt/uyarı (EDIT-P) korundu.
- ✅ **Boş-hal + progress + resx:** `.st-prod-empty` dashed; `#productWeightBar` progress; `ProductCatalogHelp`/`ProductAddPicker` yeni + `NoProductLines`/`TotalMustBe100` güncel (7 dil); `ProductAddPicker` `_IndexL10n` köprüsünde.
- ✅ **Build+test (CT izole, Release):** Diten.Web.Tests **201/0**.
- ⏳ E4 (canlı, FLEET RESTART + re-login): ürünler arama ile gelir, 400 yok, sabit satır, dashed boş-hal, progress. **KULLANICI E4 GERİ BİLDİRİMİ (→ WP-ST-EDIT-R):** (1) başlık "Ne — Ürün ve SKU karması" → **"Ürün ve SKU karması — Ne"** ({Ad}—{Soru} deseni); (2) **Eşit dağıt ikonu görünmüyor** (`bx-distribute-horizontal` bu boxicons sürümünde yok → geçerli sınıf); (3) **SKU seçimi eksik** — kullanıcıyla netleştirilecek (EDIT-P "düz ürün%" kararını tersine çevirebilir).

**WP-ST-EDIT-Q KOMPLE (E2). E4 rötuşları WP-ST-EDIT-R'de.**
