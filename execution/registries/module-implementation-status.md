# Module Implementation Status (Code-Truth)

## Purpose
Developer-facing implementation tracker: for each **code-bearing** module, its real status **derived from code** (not from docs/registry claims). Lets any developer see — before picking up work — what is built, how complete it is, and what is missing.

## Relationship to the Module ID Registry
This file is **separate from** [`module-id-registry.md`](module-id-registry.md) **on purpose**. The Module ID Registry is an **identity** registry and, by its own charter, **must never contain completion percentages or "what's done / what's missing" lists**. So all progress/status tracking lives **here** instead. The registry remains the authority for IDs/names/slugs; this file is the authority for **implementation state**.

## How to read
- **Durum** (one of): `Bitti` · `Backend+Frontend` · `Sadece-backend` · `Kısmi` · `Başlanmadı`.
- **%** is a rough **estimate** (not exact), as agreed — paired with an explicit **eksik** (what's missing) list so it's actionable.
- **Source:** code audit of `services/**`, `frontend/Diten.Web/**`, `gateway/**` (entities, controllers, handlers, tests, views, js, `.resx`). Audit date: **2026-06-23**.
- **Scope:** only modules that have code today. Not-yet-started modules from the Blueprint are NOT listed here (their IDs live in the registry / Excel `Blueprint_Data`).

---

## Platform & Shared Services — Tenant / Subscription / Catalog

| Module ID | Durum | % | Var olan | Eksik |
|---|---|---|---|---|
| MOD-0009 Tenant / Environment Mgmt (+FU01/02/03) | Backend+Frontend | 95 | Full lifecycle entity+CQRS (Register/Update/Suspend/Reactivate/Delete), 4-status model, admin-invite; Tenants UI (Index/Create/Details 4-tab/Edit/Security); tests | Per-tenant quota override form (read-only today); minor |
| MOD-0008 Enterprise Capability / Product Catalog | Backend+Frontend | 100 | ModuleCatalogItem assignable/expose, domain/service/version, search; catalog UI exposes all metadata | — |
| CAND-CAP-0002-FU01 Tenant Module Catalog | Backend+Frontend | 100 | ModuleCatalogItem + ModulePageDescriptor + actions; full CRUD UI; permission-sync; tests | — |
| CAND-CAP-0002-FU02 Subscription Plan Catalog | Backend+Frontend | 100 | SubscriptionPlan (pricing/trial/quotas/features); full CRUD UI; l10n | — |
| CAND-CAP-0002-FU03 Subscription Feature Mgmt | Backend+Frontend | 100 | FeatureDefinition/Category/PlanFeatureMapping; two-tab UI (R2); archive; l10n | — |
| CAND-CAP-0002-FU04 Module Assignment Inspection | Kısmi | 95 | Read-only inspection API (overview/plans/tenants/detail); shown in module Details | No dedicated "all assignments" admin page; read-only only |
| CAND-CAP-0002-FU05 Tenant Module Entitlements | Backend+Frontend | 100 | TenantModuleEntitlement (source/expiry/effective-access) + entitlement→bridge; Commercial tab UI; tests | — |
| MOD-0033 API Consumer & Credential / Quota Model (+FU01) | Kısmi | 75 | Quota engine (QuotaEvent/Usage, reset job, init/sync); read-only quota tab | **No quota override/governance admin UI**; **API Consumer/credential mgmt NOT implemented** (quota-only) |

## Platform & Shared Services — Admin / Org / Infra

| Module ID | Durum | % | Var olan | Eksik |
|---|---|---|---|---|
| CAND-CAP-0003 Platform Administration (+FU01/02) | Backend+Frontend | 85 | PlatformAdministrator entity; 11 API endpoints (invite/suspend/reactivate/roles/reset); full admin UI + 7-locale l10n; rules tests | MFA/TOTP setup UI; password-policy UI; hardcoded TR error strings in controller (not resx); thin handler/integration tests |
| MOD-0288 Organization, Person & Position Directory (+FU01) | Backend+Frontend | 85 | OrgUnit/Position/PositionAssignment entities + 3 controllers (CRUD + manager-chain) + cross-tenant validators; **tenant-side frontend (2026-06-23): 3 CRUD screens in tenant shell** (Views/Organization/*), nav "Organization" group, manager-chain UI. Endpoints opened to `tenant_user` actor (gateway + Platform.Common middleware exclude org paths from admin-only). **Full e2e live-proven**: LE→OrgUnit→Position→reports-to→manager-chain→assignment. | Handler/CQRS unit tests still thin; org-tree (hierarchical) view is flat DataTable for now. (was the audit's "registry done / no UI" drift — now closed.) |
| MOD-0048 Reference Data Management | Backend+Frontend | 90 | Rich entities (sets/versions/values/mappings, draft→approve→publish governance); full UI (12+ views, wizards); 7-locale l10n; audit redaction | No dedicated ReferenceData test suite (lifecycle/approval/concurrency untested) |
| MOD-0026 Scheduler / Job Orchestration | Sadece-backend | 70 | Hangfire scheduler + IBackgroundJobScheduler, JobExecutionLog, audit outbox worker; 6 test files | **No UI** (Hangfire dashboard at /jobs not integrated into nav); no job-mgmt/retry/history UI |
| MOD-0027 Notification Service (+FU02 Template UI, +FU03 Event Catalog) | Bitti | 100 | Templates/Dispatch/Settings entities; 10+ API endpoints + render-preview + dispatch list filters; SMTP provider; async dispatch job; Platform Admin UI (FU02) — NotificationTemplates/NotificationSettings (compact) + NotificationDispatches (read-only/list-detail) proxy controllers/views/JS; en+tr resx; 4 PSS-011 lookup keys; sidebar nav; 7+ test files (1112 passing). **FU02 live smoke PASS (2026-07-08)** — see [FU02 smoke audit](../../docs/audits/pss-mod-0027-fu02-notification-template-ui-smoke-2026-07-08.md). **FU03 Notification Event Catalog & Template Binding — Bitti (PASS-with-note, 2026-07-08):** `NotificationEventDefinition` entity + `ModuleManifestDocument.NotificationEvents` additive contract (integration-agent PASS, guardrails) + manifest sync/validation service + `NotificationEventsController` (events read/manage, template-slots) + read-only `/Platform/NotificationEvents` UI + `platform.notifications.events.read/manage` perms; **1139/1139 tests**; live smoke PASS (sync `providersScanned:6, eventsDeclared:0` controlled empty); see [FU03 smoke audit](../../docs/audits/pss-mod-0027-fu03-notification-event-catalog-smoke-2026-07-08.md) | Optional follow-ups (non-blocking): FU02 visual browser confirmation / restricted-actor 403 seed; **FU03 producer opt-in** (declare `notificationEvents` in Workflow/invite/campaign manifests for a populated catalog); FU03 per-event persisted validation issues; FU02 template UI consuming active-template-slots. Out of scope: FU05 tenant self-service UI, FU04 InApp/SignalR, SMS/Push (MOD-0263); DataTable verifier lacks read-only/list-detail mode (tooling follow-up). **FU03A Notification Event SourceType & PlatformSeed Bridge — Bitti (PASS-with-note, 2026-07-08):** `NotificationEventSourceType {Manifest=0,PlatformSeed=1,SystemSeed=2}` + additive `NotificationEventDefinition` fields (SourceType/OwnerArea/OwnerDisplayName/TargetRoute/ModuleCatalogRef/RequiredPolicy) + manifest-sync clobber guard (create writes Manifest; PlatformSeed/SystemSeed not clobbered) + generic `NotificationEventSeedPlanner`/`NotificationEventSeeder` + **empty** `NotificationEventSeedCatalog` + startup `NotificationEventSeed.EnsureSeededAsync` (no-op) + additive read-only DTO/mapping/UI; **1148/1148 tests** (10 new); fleet live-boot-clean smoke (Web 302, API 403, seed no-op, no 500). No tenant content, no runtime adapter, no Module Catalog/ModulePages/PlatformNavigationCatalog/Gateway side-effect; see [FU03A smoke audit](../../docs/audits/pss-mod-0027-fu03a-notification-event-sourcetype-platformseed-bridge-smoke-2026-07-08.md). Note: authenticated DTO/UI round-trip not exercised live (compensated by tests). **FU04A Tenant Management Notification Event Opt-in — Bitti (PASS-with-note, 2026-07-08):** 3 tenant events (`tenant.user.invited` / `tenant.lifecycle.suspended` / `tenant.lifecycle.reactivated`) added to `NotificationEventSeedCatalog.PlatformSeedDefinitions` as **PlatformSeed** (RequiredPolicy=PlatformActor, RequiredPermissionKey=null, TargetRoute=/Platform/Tenants, OwnerModuleId=MOD-0009) bound to existing FU02 templates; **live-seeded Active** (Mongo-verified, exactly 3, no duplicate); **1153/1148→1153 tests** (5 new). No runtime adapter, no Module Catalog/ModulePages/PlatformNavigationCatalog/Gateway side-effect, no IModuleManifestProvider, no new template; see [FU04A smoke audit](../../docs/audits/pss-mod-0027-fu04a-tenant-management-notification-event-opt-in-smoke-2026-07-08.md). Note: authenticated API JSON list/slots fetch not exercised live (Mongo persist + unit tests compensate). **FU04B (runtime eventCode → dispatch adapter) + tenant producer migration + manifest-driven workflow/document/import opt-in remain follow-ups.** (Foundation+content on MOD-0027-FU03A — not its own code-truth row per this file's charter.) **FU04B EventCode Dispatch Adapter — Bitti (PASS-with-note, 2026-07-08):** `INotificationEventDispatchAdapter` (eventCode→Active event→defaultTemplateKey→requiredVariables/recipient validation→delegate to existing `QueueEmailNotificationCommand`, returns `Response<NotificationDispatchDto>` unchanged) + thin `DispatchNotificationByEventCodeCommand`/handler + DI; template existence left to FU02 handler (platform-default fallback). **Tests 13/0** (INVALID_EVENT_CODE 400 / EVENT_NOT_FOUND 404 / EVENT_NOT_ACTIVE 409 / TEMPLATE_KEY_MISSING 422 / REQUIRED_VARIABLE_MISSING 422 / RECIPIENT_MISSING 400 / provider-failure + success passthrough + 3 tenant-event resolution); **suite 1166/0**; Platform.API 0 error. `QueueEmailNotificationCommand`/handler UNCHANGED; no producer migration; no new pipeline/provider/template; no Module Catalog/ModulePages/PlatformNavigationCatalog/Gateway side-effect; see [FU04B smoke audit](../../docs/audits/pss-mod-0027-fu04b-eventcode-dispatch-adapter-smoke-2026-07-08.md). Note: unit proof = resolution+delegation only; real render/send owned by FU02 handler. **Follow-ups: FU04C (tenant producer migration) + FU04D (producer runtime wiring) + FU04M (manifest workflow/document/import opt-in).** Adapter on Application layer — not its own code-truth row per this file's charter. **FU04C Tenant Producer EventCode Migration — pack `ready-for-dev` (registry MOD-0027-FU04C, 2026-07-09; DCP + owner review PASS; was informal "FU04B-Tenant"), NO code yet (migrate AdminUserInvitationService → tenant.user.invited + TenantLifecycleNotificationConsumer suspended/reactivated → tenant.lifecycle.* via FU04B DispatchNotificationByEventCodeCommand). Karar A RESOLVED: consumer adds TenantDisplayName (tenant.DisplayName ?? tenant.Name), mapper signature unchanged. Karar B RESOLVED: ReasonCode-based non-retryable controlled-4xx (EVENT_NOT_FOUND/EVENT_NOT_ACTIVE/REQUIRED_VARIABLE_MISSING/TEMPLATE_KEY_MISSING) log+swallow, provider/transient throw/retry, no rollback, invite fail-soft. No adapter/handler/template/seed/created-branch change. Not a code-truth row per this file's no-code-no-row charter.** |
| MOD-0021 Audit Trail Service | Backend+Frontend | 85 | Append-only AuditEvent + retention; outbox; redaction; AuditLog + AuditRetention UI; 7-locale l10n; 7 test files | Tenant-scoped audit viewer (tenant self-audit); advanced filters; UI/redaction workflow tests |
| MOD-0029-FU36 Controlled Document Registration Orchestration | Backend+Frontend | 95 | FU36A–C implemented; fleet/auth/redirect/template/direct-bypass smoke green | Runtime blocked by Language 403, Retention 500 and Controlled Documents list 500; repeat Company/Corporate/operation/reverse smoke |
| MOD-0029-FU37 Corporate/Company Registration Amendment | Backend+Frontend | 95 | FU37A–C implemented; scope guardrails remain test/verifier green | Runtime blocked before create; Corporate instance list empty and governed lookups unavailable |
| MOD-0028-FU06 Corporate Collection Instance Foundation | Kısmi | 92 | Typed Corporate scope owner; company-independent provisioning; partition/access/API foundation; Mongo-compatible positive Active partial unique index; development-only exact-index reconciliation; Platform/Mongo health 200; 5 targeted tests, 1912-test suite and verifiers green | Authenticated provisioning/idempotency/retry, explicit-grant and cross-tenant runtime smoke remain unavailable because Gateway/Auth fleet was down. FU37 remains draft but is eligible for approval review. |
| MOD-0285 System Navigation Management | Kısmi (loader done) | 55 | ModulePageDescriptor + self-reg + **runtime menu loader (2026-06-24)**: `GET /api/platform/navigation/menu` (tenant_user, entitled-modules ∩ nav-visible/Active descriptors, tenant-scoped) → Diten.Web `DynamicModuleMenuViewComponent` renders a dynamic "Modules" section in `_LayoutTenantShell` (augment — hardcoded sections untouched), per-item `Perms.Has` gating. **Live-proven in browser**: self-registered Golden Slim RECORDS appears in the tenant menu automatically (entitled+permitted), disappears when not. Closes the "code → menu" loop. | Admin menu-tree-builder + route-binding console + **governed publish/versioning** still missing (blocked on MOD-0023 Workflow); hierarchy beyond parent/child flat. |
| MOD-0002 Interface Registry | Backend+Frontend | 80 | Interface manifest import/diff/batch entities; 7 API endpoints; Index/Details UI; l10n | Only 1 test file; no visual diff renderer; no manifest-upload UI; no RabbitMQ discovery integration test |
| MOD-0035 Event Bus / Message Queue | Sadece-backend | 65 | IEventBus + RabbitMQ + InMemory fallback; lifecycle consumers; outbox; 6 integration tests | **No UI** (no DLQ/replay/subscription monitoring); unit tests for bus logic thin |

## Access Governance / Auth (Diten.AuthService + frontend)

| Module ID | Durum | % | Var olan | Eksik |
|---|---|---|---|---|
| MOD-0018 RBAC / ABAC (parent) | Backend+Frontend | 85 | Permission handler/requirement/policy provider; `[HasPermission]`; grant-source tracking; 5 admin screens | data-scope frontend; FU13 cache hooks |
| MOD-0018-FU9 RBAC Admin UI (5 screens) | Backend+Frontend | 95 | Users/Roles/Permissions/RoleAssignments/UserRoleAssignments full CRUD + grant-state JS + Vitest | RoleAssignments/UserRoleAssignments **row l10n minimal** |
| MOD-0018-FU10a Pure Auth Decision Contract | Backend+Frontend | 90 | PermissionAuthorizationHandler + AuthorizationSnapshot cache contract | AuthService self-explain (read via Platform/FU14 only) |
| MOD-0018-FU10b EntitlementChecker ResolvedFrom | Sadece-backend | 80 | EntitlementChecker + cache (Platform); integration tests | No entitlement-debug UI; batch-check TODO (perf) |
| MOD-0018-FU12 Tenant Authorization Context | Backend+Frontend | 85 | JwtTenantAuthorizationContext (lazy org-scope init); 6 test files | No "context browser" UI |
| MOD-0018-FU13 Permission Convention + Cache Invalidation | **Kısmi** | 40 | Cache key builder; event envelope | **No invalidation hook on role/perm mutation; TTL hardcoded 300s; no admin invalidate; no tests** (stale perms ≤5min) |
| MOD-0018-FU14 Effective Access Explain | Backend+Frontend | 80 | SelfAccessExplainService + SelfAccess UI (two-observation, no combined verdict by design); tested | Cross-user explain deferred |
| MOD-0018-FU15 Real DataScopeResolver | Sadece-backend | 90 | OrgDataScopeResolver (OrgUnit/Position/ManagerChain/LegalEntity from MOD-0288); tests | **No frontend** (org-scope browser); gated by MOD-0288 UI (C2) |
| MOD-0017-FU01 Tenant Login Security Settings | Backend+Frontend | 85 | TenantLoginSettings client/policy; used in handlers | **No tenant-admin config UI** (MFA/session/password policy not self-service) |
| CAND-CAP-0001 Tenant User / Identity Foundation | Backend+Frontend | 90 | User/Membership/Role/Permission entities; invitation+set-password flow; Users CRUD; ~15 tests | No bulk user import/export |

## Other services

> **MOD-0290 Exposure B evidence (2026-08-08):** LSKU MDM API/controller and nav-hidden manifest declaration is
> implemented: exactly four routes, strict two-field body, header-only idempotency, sanitized `201` projection and
> preserved non-success reconciliation `202`; permissions are only `mdm.lskus.read` / `mdm.lskus.create`. Focused
> controller, authorization and manifest tests pass `20/20`, zero skipped. This is historical B-checkpoint evidence;
> later separately authorized C-G delivery is reflected in the MOD-0290 row below, while H remains deferred.
> No-code regression verification from repository-rooted `work/lsku-b-regression` passes the previously path-failing
> root-discovery tests and the full MDM Release suite `403/403`, zero skipped; the earlier four failures were isolated
> temporary-output path failures, not LSKU Exposure B failures.

> **MOD-0290 shared personalization evidence (2026-08-09):** the Global Product, GSKU, LSKU and Finished Good
> registers now use relative browser calls to an authenticated MVC personalization proxy, which forwards server-side
> bearer/tenant context through Gateway `5000`; no Gateway route change or direct browser `5057`/`5059` call was needed.
> Live Admin save/reload/reset smoke passed on all four screens with zero console errors. LSKU's stale
> `configuration` field was aligned to Platform `viewDefinition` and verified across reload. Focused frontend tests
> pass `44/44`, JS syntax and locale parity pass, and Release build succeeds with 0 errors. Full frontend discovery is
> `143/152` passing; the nine failures are confined to six pre-existing Enterprise Strategy/Planning test files, with
> no MOD-0290/personalization failure. Viewer retains four read-only permissions (four list reads `200`, GSKU/LSKU
> create-options `403`); Admin retains four read/create pairs. Read-only Mongo proof keeps exactly
> `GS-000000000003`, `LS-000000000004` (TR) and `FG-000000000005`, each with one consumed/bound reservation. This is
> Local Development UI hardening evidence only and does not change the module percentage or close separate gates.

| Module / Service | Durum | % | Var olan | Eksik |
|---|---|---|---|---|
| MOD-0290 Product / Item / SKU Master (Diten.MdmService) | Kısmi | 18 | DCP-004 approved; Module Pack `in-progress`. Code truth now includes Global Product backend/API/Gateway/frontend/permission plus live create/read; Product Definition Revision + First GSKU; verified GSKU provider/publication, two assignments and resolver; GSKU A-G; LSKU A-G with verified `UNSD-M49-2026-08-08` 249-value market catalog; Finished Good A-E; and same-origin Save View hardening on all four registers. Retained Local Development records are `GS-000000000003`, `LS-000000000004` / `TR` and `FG-000000000005`; Admin/Viewer allow-deny and tenant-isolation evidence is recorded in the pack. Current manifest truth is Global Product/GSKU visible and LSKU/Finished Good hidden. The percentage remains unchanged because no owner-approved completion-percentage formula exists; this reconciliation does not invent one. | WorkCenter-dependent lifecycle/workflow orchestration remains open. WorkCenter-independent Market Supply Assignment, MA/Registered Presentation, packaging, artwork/label/leaflet, GTIN, remaining master-data/regulatory slices, audit transport/central acknowledgement, finite-expiry scheduling, retention/purge/redaction, metrics/runbook and Production readiness remain open. Remaining navigation decisions are deliberately deferred. |
| MOD-0290-FU01 Product Abbreviation Register (ABB) Foundation (Diten.MdmService) | Backend+Frontend | 70 | Internal ABB aggregate/lifecycle/no-reuse foundation, bounded MDM API/controller, Gateway routes and tenant register are present. FU20 now supplies the nav-hidden manifest page, exact eight global catalog definitions and six system-role grant matrices. Local Development live acceptance retained exactly two records: `QZX` CANCELLED v1 and `VWK` ACTIVE v1; non-owner cancel and same-subject checker denied, distinct Approver and Auditor flows succeeded, canceled-code reuse returned 409, and final Mongo cardinality is register 2 / ledger 2 / history 4. Disabled and expired entitlement reconciliation has live `NoAccess`/`Expired`, zero matching grants and exact active/no-expiry restore evidence. Supported credential renewal plus fresh login proved Gateway-to-MDM `403` for all eight ABB actions while disabled/expired and restored Admin/Viewer read-only behavior. Initial/final/replay role, grant, assignment and business cardinalities were identical. Focused Auth 75/75, focused MDM 57/57, full Auth 337/337 and full MDM 404/404 passed with zero skipped; browser console warning/error count was zero. Temporary responsibility memberships were removed and temporary users disabled. The percentage is unchanged because no owner-approved formula exists. | Local Development catalog/role/lifecycle onboarding, entitlement disable/expiry/restore and fresh-login HTTP closure are complete. Navigation remains deliberately deferred; workflow/worker transport, runbook/observability and Production/Staging readiness remain separately gated. No Production completion is claimed. |
| MOD-0220 Corporate Secretarial / Legal Entity (Diten.MdmService) | Backend+Frontend (slice) | 65 | LegalEntity entity + CRUD API + GET list; **tenant-side mini-slice (2026-06-23): Legal Entities screen** (create/activate/archive/delete, Views/MasterData/LegalEntities/*), gateway methods widened (POST/PATCH/DELETE), en+tr l10n. **READ-BACK BUG FIXED**: MDM was missing `clientSettings.GuidRepresentation=Standard` → all reads (list/by-id/activate/lookup-validation) returned empty against correctly-stored Standard docs; one-line fix + real-Mongo round-trip regression test. | Full legal-entity master-data UX (versions/hierarchy/wider fields) still minimal; MDM still has no launchSettings (needs ASPNETCORE_ENVIRONMENT=Development to read its dev JWT secret). |
| DEV-* Golden Slim / Compact (Diten.DevEnablementService) | Backend+Frontend | 80 | GoldenReferenceSlim+Compact full CRUD; self-reg manifest (GoldenSlimManifestProvider); 16 views; 7-locale l10n | **ZERO automated tests**; Compact less polished than Slim |
| Enterprise Strategy / Business Performance (Diten.EnterpriseStrategyService) — MOD-ID unconfirmed | Backend+Frontend (partial) | 70 | 256 .cs / 19 controllers (objectives/planning-cycles/strategy-periods/KPIs/goals/initiatives/projects); 91 views | **No l10n on 91 views**; monolithic pre-release test (`UnitTest1.cs` 1798 lines, known-red); **MOD-ID not assigned in code**; view folder name drift (`EnterpriseStrategyBusinessPerformance`) |
| MOD-0032 API Gateway (gateway/Diten.ApiGateway) | Thin proxy | 85 | Ocelot 69 routes; JWT handler (+ PreviousSecrets rotation); correlation-id; Serilog/Prometheus | No tests; hardcoded downstream hosts (localhost:505x); no rate-limiting; routes undocumented |

---

## Foundation gaps to stand up BEFORE vertical modules (HR/CRM, MOD-0297+)

Ordered by how much they block verticals:

1. 🔴 **MOD-0288 Org/Person/Position — frontend.** Backend done, UI absent. Data-scope (FU15) cannot be operated; verticals needing org structure/data-scope are blocked. (roadmap C2)
2. 🔴 **MOD-0023 Workflow + MOD-0024 Task — not built (0%).** Any vertical needing approvals/SLA/tasking (and MOD-0285 governed publish) is blocked until ≥ MVP.
3. 🔴 **MOD-0285 Navigation runtime loader.** Until built, every new module needs a hand-edited hardcoded menu entry.
4. 🟡 **MOD-0220 MDM Legal Entity — frontend.** Backend-only; HR/CRM commonly reference legal entities → needs UI.
5. 🟡 **MOD-0033 Quota override admin UI** + **MOD-0018-FU13 cache invalidation** (stale-perm correctness) + **MOD-0017-FU01 tenant security self-service UI**.
6. 🟡 **MOD-0014 Module Boundary Registry — not built** (only needed if menu/grouping must be formal-boundary-driven; catalog-domain is a lighter substitute).

## Quality debt (cross-cutting)

- **Tests missing:** DevEnablement (0), API Gateway (0); EnterpriseStrategy pre-release monolith (known-red); MDM sparse.
- **L10n missing:** EnterpriseStrategy (91 views), MDM (all).
- **Backend-without-frontend:** MOD-0288, MOD-0220, MOD-0026 (ops), MOD-0035 (infra), MOD-0018-FU15.

## Access Governance — partial items the owner flagged (confirmed in code)
- MOD-0018-FU13 cache invalidation: **no frontend, no tests** (40%).
- MOD-0017-FU01 tenant login security: **no self-service config UI** (85% backend).
- MOD-0018-FU15 data-scope: **no frontend** (gated by MOD-0288 UI).
- RoleAssignments / UserRoleAssignments: **row-level l10n incomplete**.
- (The 5 core RBAC CRUD screens themselves ARE built + tested — 95%.)

## Commercial Suite (CRM + O2C) — Reserved, no code yet

> Exception to the "code-bearing only" scope: listed here as an explicit **not-started** pointer so pickers see the reservation. **No code exists for any of these** — `implementation: not-started`, `runtime service: not-created` (`services/Diten.CrmService/` does not exist), `module pack: not-created`. Identity reserved in [module-id-registry.md](module-id-registry.md) (DCP-002 gate OK 2026-07-14); domain scaffold at [execution/domains/commercial-suite/](../domains/commercial-suite/). Common note: *"Commercial Suite domain foundation completed; individual module pack required before implementation."*

| Module ID | Durum | % | Var olan | Eksik / Not |
|---|---|---|---|---|
| MOD-0149 Customer 360 / Account Hierarchy | Review-ready | 95 | **Backend Account foundation** in `services/Diten.CrmService` (5-layer scaffold @5061; Account/ExternalReference/AttributeValue/CodeSequence entities; CQRS create/update/delete/bulk-delete/link-unlink-parent/upsert-attribute + list/by-id/overview/hierarchy; AccountCode `ACC-{YYYY}-{seq}` tenant+year gen; Mongo indexes; `[HasPermission("crm.account.*")]`; MOD-0048 reference seam; MOD-0021 audit seam; 13 unit tests green; build 0/0). Gateway route `/api/crm/accounts*`→5061 added; `crm.account.*` permissions seeded (DataSeeder) + tenant-Admin auto-grant (`AdminModules += crm-account`, AuthService 289 tests green). | **Frontend** (`Views/CRM/Accounts/**` compact + menu) not built; **MOD-0021 audit HTTP wiring** (logging seam only); **import/export** endpoints deferred; **MOD-0048 `account-type`/`account-status` sets not published** → create-validation blocked; **AuthService re-seed run** (new keys reach live catalog) pending; **live authenticated golden-flow** not proven. **Runtime-readiness 2026-07-14 (live Mongo): blocker B CLOSED, A OPEN** — AuthService re-seed ran: `crm.account.*` = **9/9 in `diten_auth_v3.permissions`** + **9/9 granted** in rolePermissions (SuperAdmin + tenant Admin via `AdminModules += crm-account`). MOD-0048 `account-type`/`account-status` — live governance flow executed 2026-07-16 (authenticated steward): both sets **created + values loaded (9/5) + validated + submitted**, but **NOT published** — blocked at approve by **segregation-of-duties** (`sod_submitter_cannot_approve`; needs a 2nd tenant-97c5 approver). PublishedVersionId=null. **LIVE GOLDEN FLOW PROVEN 2026-07-16 (PASS):** both reference sets published in tenant 97c5 → `published-values?scope_key=` returns **account-type 9 / account-status 5**; **Gateway create smoke 8/8** — create with empty AccountCode → **`ACC-2026-000001`**, list, 360 overview (`coverage=not-available/MOD-0151`), duplicate→409, invalid AccountType→**400 via live MOD-0048 validation**, unknown→404, soft-delete→404 on reload. Fixes landed: MOD-0048 `MatchesScope` ScopeKey, CRM `scope_key` + `data.items[]` parser, CRM Guid→string class maps (EntityBase). Tests 19/19. **FRONTEND COMPACT VERTICAL DELIVERED + BROWSER GOLDEN FLOW PROVEN 2026-07-17 (PASS):** `Controllers/CRM/AccountsController.cs` (Gateway-only proxy; Bearer+`X-Tenant-Id` propagation; `GatewayResponse<T>`→ModelState/TempData), `Models/CRM/AccountViewModels.cs` (17 fields; no Zone/MicroZone/Territory/SalesRep), full compact views `Views/CRM/Accounts/**` (Index/Create/Edit/Details+360/_Form/_Filter/_DataTable/_IndexL10n), `wwwroot/assets/js/CRM/Accounts/{index,index.l10n,form}.js` (DataTable v2, `window.API?.crm ?? window.ApiBaseUrl`, no direct 5061), 7-language resx (49 keys parity), tenant-shell menu `<li>` gated by `crm.account.read`. Gates: Diten.Web build 0/0, **compact verifier 94/0 PASS**, RESX 7-lang parity PASS. Live browser flow (tenant-97c5 CRM Admin cookie session): login→`/CRM/Accounts` 200→lookups **9 type/5 status from MOD-0048**→list via Gateway→Create empty AccountCode→**`ACC-2026-000002`** auto-gen→Details/360 (code+address+`MOD-0151` coverage placeholder)→duplicate **409**, invalid type **400**, unknown id **404/redirect**, soft-delete→hidden — all user-friendly. Bug fixed mid-flow: published-values items use `code`/`label` (not `valueCode`/`displayName`) → model now binds both. Follow-ups: import/export, MOD-0021 audit HTTP wiring, Platform MatchesScope unit tests. **PLATFORM CATALOG / NAV DESCRIPTOR / MVC PERMISSION HARDENING 2026-07-17 (PASS):** (1) `AccountsController` per-action MVC guards added (`PermissionClaims.HasPermission` inline → bare 403 re-executed to `/Home/Status/403` for pages, JSON 403 for API) — Index/Details/GetById/Lookups=`crm.account.read`, Create GET+POST=`crm.account.create`, Edit GET+POST=`crm.account.update`; no action left with only `[Authorize]`. (2) Catalog: the operator's Manual `CRM` item was reconciled to **Origin=SelfRegistered** via a new Platform-side `CrmManifestProvider` (mirrors Organization/Workflow cross-service providers; **no manifest forced into CrmService**), preserving SOFT operator fields (**IsTenantAssignable=true** already correct, Domain=SALES). (3) Page descriptor **created**: `/CRM/Accounts` → `crm.account.read`, `IsNavigationVisible=false` (static tenant-shell menu owns nav; nav-flip + static-`<li>` removal is the MOD-0285 data-driven migration follow-up — verified no double-menu: dynamic nav excludes CRM). Live: authorized 97c5 admin 200; unauth 302→login; non-tenant/permission-less actor 403; CrmService 19/19; Web/Platform/Gateway/CRM builds 0/0. Governance follow-ups: catalog `Service=DITENPPMSERVICE` legacy value (SOFT, operator-owned) and module-code `CRM` vs permission-module `crm.account` naming; MOD-0285 nav migration. **OPEN GAP — External Reference:** UI captures a single external id with `SourceSystem` hardcoded `"default"`; deferred = SourceSystem dropdown (MOD-0048-sourced) + multiple external references per account + lookup-by-external-id endpoint — see [external-reference-gap](../../docs/audits/mod-0149-external-reference-gap.md). **FINAL REVIEW / HARDENING CLOSEOUT 2026-07-17 (PASS → Review-ready):** re-verified end-to-end — CrmService build 0/0 + tests 19/19, Gateway/Platform/Web builds 0/0, compact verifier 94/0, RESX 7-lang parity (52 keys), published-values account-type 9 / account-status 5, catalog CRM Origin=SelfRegistered + IsTenantAssignable=true, page descriptor `/CRM/Accounts`=`crm.account.read` nav-visible=false (no double-menu), permission guard authorized 200 / unauth 302 / non-tenant-or-permissionless 403, browser golden flow create empty→**ACC-2026-000006**→Details/360→duplicate 409→invalid 400→unknown 404→soft-delete hidden. Boundary clean (no Zone/MicroZone/Territory/SalesRep, no 360.read, no direct 5061, no offcanvas/quickview, no CRM local seed, no hardcoded fallback). MOD-0048 location sets also published live by the operator (country 15 / city 81 / district 9-Edirne-starter / account-category 6). Remaining items are non-blocking follow-ups (MOD-0285 nav migration, catalog Service legacy value, CRM↔crm.account naming, import/export, MOD-0021 audit HTTP, MatchesScope unit tests, external-reference gap, dev residue). See [final-review-hardening-closeout](../../docs/audits/mod-0149-final-review-hardening-closeout.md). Small CRM follow-ups (incl. **GAP-CRM-01 Accounts list Country/City filters**) tracked in [followups-backlog](../../docs/audits/mod-0149-followups-backlog.md). See [correct-tenant-create-smoke](../../docs/audits/mod-0149-correct-tenant-create-smoke.md) + [frontend-compact-vertical](../../docs/audits/mod-0149-frontend-compact-vertical-golden-flow-2026-07-17.md) + [platform-catalog-nav-permission-hardening](../../docs/audits/mod-0149-platform-catalog-navigation-permission-hardening.md). **POST-CLOSEOUT ENHANCEMENT — Account 360 UI Completion (PARTIAL) 2026-07-20** ([audit](../../docs/audits/mod-0149-mod-0150-account-360-ui-completion.md)): Customer 360 Details.cshtml extended with MOD-0150 FU03 **Related Contacts** + FU04 **Related Accounts** read-only projection cards (Gateway-only via `related-contacts`/`related-accounts` endpoints, permission-gated `crm.account-contact.read`/`crm.account-relationship.read`, empty/unauthorized/dependency states, inverse relationship label rendered from this account's perspective); frontend-only VM extension (no Account-entity pollution), 19 new keys × 7-lang RESX (71/71 parity), Web build 0 errors, compact verifier PASS. PARTIAL = authenticated 97c5 browser render smoke deferred. No backend/permission/route/registry-ID change. **RENDER SMOKE → PASS 2026-07-20** (live 97c5: real Related Contacts/Accounts rows, inverse "serves", empty-state; see MOD-0150 row). **RELATIONSHIP MANAGEMENT UI + HISTORICAL LIFECYCLE PASS 2026-07-21** ([audit](../../docs/audits/mod-0149-mod-0150-account-360-relationship-management-ui-historical-lifecycle.md)): Account Details Add/Edit/**End** for Related Contacts + Related Accounts over existing MOD-0150 APIs; End never deletes (Status=ended+ValidTo, history preserved); backend historical-lifecycle remediation (repo active-checks + partial-index migration + contact-link Status + validity→400); CrmService 82/82, RESX 95×7, verifier PASS, live golden flow + failure paths green. Details.cshtml gains inline management (manage-gated). (Blueprint W-1) |
| Diten.CrmService (MOD-0149-PREREQ scaffold) | Sadece-backend | 60 | 5-layer service @port 5061; boot/build/smoke PASS; Account foundation hosted (see MOD-0149 row) | No frontend/gateway-owned surface; runtime service scaffold pack `MOD-0149-PREREQ` ready-for-dev |
| MOD-0150 Contact & Relationship Management | Closeout PASS (Review-ready) | 100 | — | **Final Validation / Closeout Gate PASS 2026-07-20** ([audit](../../docs/audits/mod-0150-final-validation-closeout.md)) — FU01–FU06 (+FU05) uçtan uca doğrulandı: **CrmService tests 77/77**, 6 proje build 0 hata (CrmApi/Gateway/Auth/Platform/Web + AuthService seed), **compact verifier 94/0**, RESX 7-lang parity (52 key). Capability closeout matrisi tam (Contact master/refs/list/search/detail/overview, compact vertical, AccountContactLink + projeksiyonlar, AccountRelationship + metadata direction/inverse/selfAllowed + Related Accounts, import/export/template, audit HTTP-ready, consent/preference read-only seam). Reference validation: 5 set MOD-0048'den (prior FU canlı smoke'larla kanıtlı), validator testleri SetMissing/InvalidValue/**Deprecated** kapsıyor, **local/hardcoded fallback yok**. RBAC: 13 izin anahtarı seeded (PKS-001 valid, `crm.contact.360.read`/`relationship.360.read` **yok**), 97c5 grant üç prefix'i (`crm.contact.`/`account-contact.`/`account-relationship.`) kapsar. Gateway: 4 CRM route'u tüm FU endpoint'lerini kapsar, **18/18 route unauth 401** (routed+guarded, 404 yok), yeni route yok. Boundary/SoR guard'ları temiz (Account/Contact entity kirliliği yok, consent-capture yok, Zone/Territory/SalesRep yok, local seed yok, MOD-0164 fake HTTP yok). **Post-closeout enhancement (blocker değil):** Account Details Related Contacts/Accounts render + Relationship/Link/Import-Export management UI (→ Account 360 UI Completion **— render DELIVERED (PARTIAL) 2026-07-20** ([audit](../../docs/audits/mod-0149-mod-0150-account-360-ui-completion.md)): MOD-0149 Details.cshtml now renders FU03 Related Contacts + FU04 Related Accounts read-only projections, Gateway-only, gated by `crm.account-contact.read`/`crm.account-relationship.read` (no new permission), empty/unauthorized/dependency states, RESX 71/71 × 7-lang, Web build 0 errors, compact verifier PASS; **PARTIAL** = authenticated 97c5 browser smoke deferred (needs running fleet+token); closeout status unchanged; management UI still open) **— browser golden flow remediation 2026-07-20 → PARTIAL → PASS**: attempt-1's "reference sets not published" was a **false negative** (login without `X-Tenant-Id` resolved user to default tenant `0…01`; corrected by logging in with `X-Tenant-Id: 97c5` → JWT tenant 97c5 / Admin / 117 perms). 97c5 reference sets ARE published (account-type 9, account-status 5, contact-type 9, contact-status 4, contact-role 7, account-relationship-type 6, account-relationship-status 4). **Live golden flow PASS:** created test data via Gateway API (Contact "Dr. Ayse Yilmaz" + decision-maker/primary link to hospital ACC-2026-000004 + `served-by` relationship to pharmacy ACC-2026-000003); projections return real rows; **authenticated web render (tenant 97c5)** of `Details/{hospital}` shows Related Contacts row (name/type/role/**Primary** badge/phone/email + Contact link) + Related Accounts row (ACC-2026-000003/served-by/direct + Account link); `Details/{pharmacy}` shows **inverse "serves"** + empty-state "No related contacts yet."; headings localized (RESX loaded, no raw-key leak); **0 direct-5061** in rendered HTML; permission guard live (no-token 401, authorized 404-not-403/200). Nav: Commercial Suite → Accounts/Contacts render for 97c5 Admin; Related Contacts/Accounts are read-only sections **inside** Account Details (no separate nav item by design). Boundary clean (no seed/mock/new-permission/backend-change). Only open item: limited-user unauthorized live render (Low; code-proof stands). **— Account 360 Relationship Management UI + Historical Link Lifecycle PASS 2026-07-21** ([audit](../../docs/audits/mod-0149-mod-0150-account-360-relationship-management-ui-historical-lifecycle.md)): Account Details now supports **Add/Edit/End** for Related Contacts (AccountContactLink) + Related Accounts (AccountRelationship) via existing MOD-0150 APIs (Gateway-only, full-page child actions `/CRM/Accounts/{id}/Contacts|Relationships/{Add,Edit,End}`, `contact-role`/`account-relationship-type`/`account-relationship-status` dropdowns from MOD-0048, manage-gated `crm.account-contact.manage`/`crm.account-relationship.manage`). **Historical lifecycle: End never deletes** — PUT sets `Status=ended`+`ValidTo`, record kept in projection; same-key re-activation allowed. **Backend historical-lifecycle remediation** (allowed, minimal): `RelationshipLifecycle` helper; repo active-checks exclude ended/inactive; **Mongo partial unique index migration** (`ux_account_contact_links_active_natural`/`_primary`/`ux_account_relationships_active_directional` → `IsDeleted=false AND Status="active"`, idempotent drop+recreate) — fixed E11000-on-relink; contact-link Update accepts `Status`; `ValidationBehavior` → controlled 400 (ValidFrom>ValidTo was 500). **CrmService tests 82/82** (+5), builds 0 errors, RESX **95×7**, compact verifier PASS. **Live 97c5 golden flow**: Add→302, End→Status=ended+record preserved, re-link same key→201, history survives fleet restart (Dr. Ayse decision-maker ended+active; A↔B served-by ended+active), inverse "serves" on target; failure paths friendly (invalid role/type/status 400, duplicate/second-primary/bidirectional-reverse 409, self-link 400, validity 400). Boundary clean (no hard delete — repos ReplaceOne only; no new permission/reference-set/seed; no direct 5061; no Zone/Consent/graph). Open: live limited-user render (Low, code-proof), date-range interval-overlap (Low). Downstream historical-context rule recorded (MOD-0153/0154/0155/0168 must snapshot link-id/context). **Next: MOD-0151 Territory Management Pack Prep.** MOD-0285 dynamic-nav migration, `Crm:Audit:Mode=http` runtime cutover, kimlik-doğrulamalı içerik smoke'u (runtime 97c5 token gerekir). **Next: MOD-0149/MOD-0150 Account 360 UI Completion.** — **FU05 Consent / Preference Seam PASS 2026-07-20** ([audit](../../docs/audits/mod-0150-fu05-consent-preference-seam.md)) — read-only MOD-0164 consent/preference **seam** on Contact 360: `IContactConsentPreferenceReader` + `NullContactConsentPreferenceReader` (default no-op, **fabricates no state, no network call**) + `ContactConsentPreferenceSummaryDto` (NotAvailable/NotAuthorized factories); `ContactOverviewDto` extended with `ConsentPreferenceSummary`; `GetContactOverviewHandler` **fail-soft** (reader throw → not-available, 360 never breaks) + **permission-masked** (no `crm.contact.consent.read`/`preference.read` → not-authorized, reader not even called → no leak); controller resolves the two perms from claims via new `PermissionClaims.HasPermission`; Contact Details placeholder wired to seam (read-only table; **no capture/toggle/grant/revoke**); `crm.contact.consent.read`/`crm.contact.preference.read` seeded (97c5 grant auto-covers via `crm.contact.` prefix); **CrmService tests 77/77** (+5 FU05), CRM Api/AuthService/Web builds 0 errors, RESX 7-lang parity (52 keys, +5); live overview route 401 (routed+guarded). Boundary clean (no consent engine/capture/approval, no MOD-0164 hard dependency, no fake data, no import/export or AccountContactLink/AccountRelationship change, no Zone). Open: authenticated browser smoke needs runtime 97c5 token + AuthService restart to apply the new grant; MOD-0164 HTTP reader is a future config-gated upgrade. **Next: MOD-0150 Final Validation / Closeout Gate.** — **FU06 Import / Export / Audit Hardening PASS 2026-07-20** ([audit](../../docs/audits/mod-0150-fu06-import-export-audit-hardening.md)) — Contact / AccountContactLink / AccountRelationship **import** (JSON rows → validated `ImportResultDto` summary, per-row errors, **dry-run/validate-only**) + **export** & **import-template** (CSV, RFC-4180 writer); MOD-0048 reference validation **cached per distinct (set,value)** during import (never a local fallback); account/contact resolution by id **or** code/external-ref; conflict rows (duplicate external-ref, duplicate/primary link, self-link, bidirectional-reverse) reported as row conflicts (not 409); **MOD-0021 audit seam made HTTP-ready** (`HttpCrmAuditPublisher` → governed `POST /api/v1/platform/audit/events`, **fail-soft + PII-safe/counts-only**, opt-in via `Crm:Audit:Mode=http`; default logging seam unchanged); routes under existing Gateway wildcards (`/api/crm/contacts/*`, `/api/crm/accounts/contact-links/*`, `/api/crm/accounts/relationships/*`) — no new Gateway route; permissions `crm.contact.import/export`, `crm.account-contact.read/manage`, `crm.account-relationship.read/manage` (already seeded+granted); **CrmService tests 72/72** (+11 FU06), Api build 0/0; live Gateway smoke: all 9 routes 401 (routed+guarded, not 404). Boundary clean (no bulk-merge/dedup engine, no async job queue, no Consent/Zone). Open: authenticated happy-path smoke (dry-run summary + CSV round-trip) needs a runtime 97c5 token; actual `Crm:Audit:Mode=http` runtime cutover deferred. **Next: FU05 Consent/Preference Seam.** — **FU04 Account-to-Account Relationships PASS 2026-07-17** ([audit](../../docs/audits/mod-0150-fu04-account-to-account-relationships.md)) — `AccountRelationship` directional aggregate + CRUD + account existence + account-relationship-type/status validation + **metadata-driven direction/inverse-display/self-link** (new `IReferenceMetadataReader` seam reads published-value attributes) + duplicate (bidirectional-aware, reverse pair 409) + Account 360 Related Accounts projection with inverse label; `crm.account-relationship.read/manage` seeded+granted; API under existing Gateway wildcard; **CrmService tests 62/62**, builds 0/0; live smoke create→direct/inverse display (target sees "serves")→duplicate 409→bidirectional-reverse 409→self-link 400→invalid type/status 400→delete PASS. account-relationship-type(6+metadata)/status(4) now published by operator. Boundary clean (no relationship graph/Consent/Zone; no Account-entity pollution). **Next: FU05 Consent/Preference Seam or FU06 Import/Export/Audit Hardening.** — **FU03 Account Contact Links PASS 2026-07-17** ([audit](../../docs/audits/mod-0150-fu03-account-contact-links.md)) — `AccountContactLink` M:N aggregate + CRUD + Account/Contact existence + contact-role validation + duplicate/primary-per-(Account,Role) uniqueness (409) + Account 360 Related Contacts / Contact 360 Linked Accounts projections (Contact Details wired to real data); `crm.account-contact.read/manage` seeded + granted to 97c5; API under existing Gateway wildcards; **CrmService tests 45/45**, all builds 0/0; live smoke link→projections→dup 409→2nd-primary 409→invalid-role 400→delete→excluded PASS. Status = free internal marker (no account-contact-link-status set yet). Boundary clean (no AccountRelationship/Consent/Zone; no Account-entity pollution). **Next: FU04 Account-to-Account Relationships.** — **FU02 Contact Frontend Compact Vertical PASS 2026-07-17** ([audit](../../docs/audits/mod-0150-fu02-contact-frontend-compact-vertical.md)) — Contacts list/create/edit/details/360 (Golden Reference Compact), contact-type/status dropdowns from MOD-0048, static menu `<li>` (crm.contact.read) + CrmManifestProvider CONTACTS page descriptor (/CRM/Contacts, nav-visible=false), per-action MVC permission guards; compact verifier 94/0, RESX 7-lang parity (43 keys), CrmService tests 32/32, all builds 0/0; live browser golden flow create→auto-DisplayName "Mehmet Demir"→details→invalid 400 friendly→delete→404 PASS. Backend fix: CreateContact/UpdateContact LastName nullable (FirstName-only contacts). Boundary clean (no AccountContactLink/AccountRelationship/Consent-capture/Zone UI; placeholders read-only). **Next: FU03 Account Contact Links.** — **FU01 Contact Foundation Backend PASS 2026-07-17** ([audit](../../docs/audits/mod-0150-fu01-contact-foundation-backend.md)) — Contact aggregate + CRUD + external refs + MOD-0048 validation (contact-type 9/contact-status 4) + `crm.contact.*` permissions (seeded + granted to 97c5 Admin) + Gateway `/api/crm/contacts` route; **CrmService tests 32/32**, builds 0/0; live Gateway smoke create→auto-DisplayName→detail/overview→invalid 400→duplicate 409→soft-delete→404 all PASS; boundary clean (no AccountContactLink/AccountRelationship/Consent/Zone). FU03/FU04 sets (account-relationship-type/status) still pending publish. **Next: FU02 Contact Frontend Compact Vertical.** — **Pack approved + MOD-0150-PREREQ complete 2026-07-17** ([MOD-0150 pack](../domains/commercial-suite/module-packs/MOD-0150-contact-relationship-management.md)) — D1–D7 approved; boundary/permission(PKS-001)/reference gates PASS. **MOD-0048 required-set authoring template ready** ([template](../../docs/audits/mod-0150-required-reference-authoring-template.md) · [json](../../docs/audits/mod-0150-required-reference-authoring-template.json)) — counts 9/4/7/6/4, kebab, account-relationship-type metadata. Status **ready-for-dev**, `runtime_code_allowed=true` scoped **FU01-contact-foundation-backend-only**. **Operator must publish the 5 required sets before the FU01 create smoke** (controlled 400 until then) — runtime prerequisite, not a code blocker (MOD-0149 parity). Owns Contact + AccountContactLink + AccountRelationship on `Diten.CrmService`; builds on MOD-0149; Consent → MOD-0164 read-only seam. **Next: MOD-0150-FU01 Contact Foundation Backend** (create smoke gated on operator publish). **Reference publish exec 2026-07-17 (PARTIAL):** all 5 required sets **created + submitted** under 97c5 (contact-type 9 / contact-status 4 / contact-role 7 / account-relationship-type 6 +metadata / account-relationship-status 4); **approve+publish blocked by SoD** (submitter=bestepullukcu, no 2nd-approver credential) — needs a second 97c5 approver, then publish + published-values smoke. See [reference-publish-execution-verification](../../docs/audits/mod-0150-reference-publish-execution-verification.md). **— Contact Location & PII/KVKK Hardening PASS 2026-07-21** ([audit](../../docs/audits/mod-0150-contact-location-pii-kvkk-hardening.md)) — post-closeout enhancement (closeout %100 unchanged, **no new module/permission/reference-set/seed**). (1) **Location model:** `Contact` + Create/Update command/validator/mapper/`ContactDetailDto`'ya 7 opsiyonel alan (`CountryRef/CityRef/DistrictRef/AddressLine/PostalCode/PreferredLanguage/PhoneCountryCode`); country **zorunlu değil** (Details/Edit "Location incomplete" rozeti); opsiyonel `country/city/district` MOD-0048'den doğrulanır (SetMissing tolere, InvalidValue→400, **no CRM local seed**); phone-country-code + preferred-language shape validation; sparse `ix_contacts_tenant_country`. (2) **Cross-country = controlled relationship (Seçenek 1 soft-warning + reason-required):** `CrossCountryPolicy` — iki tarafın da bilinen+farklı country'si varsa reason zorunlu (yoksa **controlled 400**), country bilinmiyorsa **bloklanmaz**; `AccountContactLink` + `AccountRelationship` command/request/entity'ye `CrossCountryReason`; audit'e yalnızca country kodu (`crossCountry=TR->US`), **reason metni asla loglanmaz**. (3) **PII/KVKK:** `PiiMasking.Redact` (e-posta→`***@***`, 7+ hane telefon→`***`, GUID/country korunur) audit publisher'a uygulandı; Contact create/update/**delete** + link/relationship audit call-site'ları **raw isim/telefon/e-posta taşımıyor** (entityId+corrId); import/export zaten counts-only/row#+field. (4) **Notes safety + PII/KVKK notice** Create/Edit + ContactLink/Relationship formlarında (7 dil). **Gates:** CrmService **tests 98/98** (+21 `ContactLocationPiiHardeningTests`), Web/CrmApi/Gateway build 0 hata, **compact verifier 94/0** (section parity korundu), RESX parity (ContactIndex 65×7, AccountIndex 98×7), search guards CLEAN (PII/hardcoded-location/5061/patient/consent/territory/seed yok). **Karar kayıtları:** identity/communication fields = PII (raw audit yasak); cross-country = controlled (silent default değil); **Patient ≠ sıradan Contact** (dedicated healthcare privacy scope olmadan CRM genel closeout tamamlanmaz — açık **High** follow-up); location hardening MOD-0151 Territory + MOD-0155 Visit için hazırlık (o modülleri implement etmez). Boundary temiz (no Zone/Territory/SalesRep/Visit/Patient/Consent-engine, no hard delete, no direct Mongo). Open: authenticated browser smoke (Low, kod-proof), `crossCountryPolicy` type-metadata + cross-country override permission (follow-up, bu taskta icat edilmedi), Patient/Healthcare Privacy Scope (High governance). **— Contact Import/Export Task 1: XLSX Template + Reference Helper + Existing-Data Export PASS 2026-07-23** ([audit](../../docs/audits/mod-0150-contact-import-export-task1-xlsx-template-export.md)) — post-closeout enhancement (closeout %100 unchanged; **no new module/permission/reference-set/seed/Gateway route**). (1) **Tek kaynaklı şema** `ContactWorkbookSchema` — Contacts 23 kolon (`Operation, ContactId, ExternalSystem/ExternalId, ad alanları, ContactType/ContactStatus, Gender, professional alanları, location alanları, Phone/Email, Notes`) + AccountLinks 16 kolon (`Operation, LinkId, ContactId, Contact external ref, AccountId/AccountCode/AccountName, RoleCode, IsPrimary, Status, ValidFrom/ValidTo, ReportsToContactId, Notes, CrossCountryReason`); template ve export **aynı** şemayı kullanır → round-trip'e hazır. `ContactCode`/`PhotoDataUri`/teknik alanlar **eklenmedi** (entity'de yok / PII). (2) **XLSX** (`format=xlsx`, **ClosedXML 0.102.2 dependency added**): `import-template?format=xlsx[&includeAccounts]` → Instructions/Contacts/AccountLinks/ReferenceData (+ `crm.account.read` varsa Accounts lookup); Instructions'ta Operation sözlüğü, sistem kolonları uyarısı, **historical-end kuralı (silme değil `Status=ended`+`ValidTo`)**, PII/KVKK + Notes/hasta-verisi yasağı, "import upload/apply Task 2'de" notu, referans set durumu. **CSV yolu birebir korundu** (`format` yoksa/`csv` ise FU06 davranışı). (3) **ReferenceData helper + dropdown:** yeni `IReferenceDataCatalogReader` seam'i (aynı Gateway `published-values?scope_key=` consumer'ı, **liste** okuması) 12 seti çeker; 11 kolona in-cell data validation **ReferenceData aralığından** bağlanır; yayınlanmamış set → `NOT_PUBLISHED` satırı + **dropdown yok**; deprecated değer işaretlenir ve seçilemez; **hardcoded fallback yok**. `account-contact-link-status` seti **icat edilmedi**. (4) **Export:** `export?format=xlsx&includeLinks&includeHistorical&includeNotes&includeAccounts&contactType&status&country&updatedAfter` — `ContactId`/`LinkId`/`AccountId` dolu, external-ref dolu, location+Gender dahil, `Operation` boş; **historical linkler yalnız `includeHistorical=true` ile**; **Notes + `CrossCountryReason` opt-in** (varsayılan boş); satır limitleri (5000/20000/2000) aşılırsa **controlled 400 + filtre önerisi**; `includeLinks`/`includeAccounts` için `crm.account-contact.read`/`crm.account.read` **fail-closed 403**. (5) **UI:** Contacts DataTable'ının mevcut **Action dropdown'ı içinde** (Print/CSV/Excel/PDF/Copy grubunun altında, ayırıcıdan sonra Import ile aynı grupta) permission-aware **Download Template** + **Export Contacts** (MOD-0013 premium diyalog: PII/KVKK uyarısı, includeLinks/Historical/Notes/Accounts, grid filtresi taşınır); Import öğesi **sahte akış başlatmaz** (`ImportComingSoon`); Gateway-only MVC proxy (`/CRM/Contacts/template|export`), direct 5061 yok; `dt-defaults.js`'e additive `extraButtons.collectionBtns` (yalnız `importBtn` geçen mevcut modüllerde çıktı birebir aynı). (6) **PII/KVKK:** export audit'i yalnız sayaç/bayrak/**filtre alan adı** taşır (değer/isim/e-posta/telefon/not yok, testle kanıtlı), dosya adı `contacts-export-{tenantShort}-{ts}-{corr8}.xlsx`. **Gates:** CrmService **tests 128/128** (+23 `ContactWorkbookExportTests`), CrmApi/Web/Gateway build 0 hata, **compact verifier 94/0**, RESX **ContactIndex 85×7**, canlı smoke: Gateway xlsx route'ları **401**, frontend proxy **302→login**, çalışan 5061 OpenAPI'sinde yeni parametreler görünür. Boundary temiz (import parse/dry-run/apply yok, AccountRelationship XLSX yok, yeni permission/set/seed yok, hard delete yok, Patient/Territory/Visit/Consent yok). Open: kimlik-doğrulamalı indirme smoke'u (Low, kod+unit+route proof), Instructions sayfası şu an yalnız İngilizce (Medium), bellek içi filtreleme + sabit satır limitleri (Medium/Low), `RowVersion` concurrency kolonu (Task 2 kararı), Patient/Healthcare Privacy Scope (**High**, açık). **— Contact Import/Export Task 2: XLSX Upload + Dry-run Preview + Safe Apply PASS 2026-07-23** ([audit](../../docs/audits/mod-0150-contact-import-export-task2-xlsx-upload-dryrun-apply.md)) — post-closeout enhancement (closeout + Task 1 %100 unchanged; **no new module/permission/reference-set/seed/Gateway route**). Round-trip döngüsü kapandı: **indir → düzelt → yükle → önizle → uygula**. (1) **Upload:** `POST /api/crm/contacts/import-file?dryRun=true&strictMode=` + `/import-file/apply` (multipart, 10 MB, `.xlsx`); **FU06 JSON import + CSV export/template + Task 1 XLSX export/template birebir korundu** (canlı: import 405 POST-only, export/template 401). Staging/idempotency token yok — apply'da dosya yeniden gönderilir (follow-up). (2) **Parser** (`ContactWorkbookReader`): Task 1 şemasına göre normalize başlık eşleşmesi (kolon sırası değişebilir), **gerçek Excel satır numarası**, sayıya/tarihe dönmüş hücrelerin metin/ISO geri dönüşümü (telefon/posta kodu/harici id bozulmaz), boş satır atlama, bilinmeyen kolon **uyarı**, zorunlu kolon eksikse **dosya-seviyesi hata**; **ReferenceData sayfasına güvenilmez** — doğrulama daima MOD-0048 published-values. (3) **Operasyonlar:** Contacts `add`/`update`/`skip` (delete **desteklenmez** → controlled error), AccountLinks `add`/`update`/**`end`**/`skip`. Eşleştirme: Contact = `ContactId` → `ExternalSystem`+`ExternalId`; Link = `LinkId` → (Contact+Account+RoleCode aktif). **E-posta/telefon ile eşleştirme yapılmaz** (benzersiz değil, yanlış kişiye yazma riski — testli). Boş `Operation` = **skip** (export dosyası hiçbir şeyi değiştirmez). Boş hücre = değiştirme, `<CLEAR>` = temizle; `ContactId`/`AccountId` link üzerinde **immutable**. (4) **Historical lifecycle:** `end` → `Status=ended` + `ValidTo`, `IsDeleted=false`, kayıt korunur; kişi A→B taşınması = eski link end + yeni link add (2 kayıt); ended link yeni aktif linki **bloklamaz**; hard delete/overwrite yok; downstream satış/ziyaret/sipariş/rota bağlamı yeniden atanmaz. (5) **Cross-country parity:** `CrossCountryPolicy` artık import add/update yolunda da uygulanıyor — **Task 1 analizinde tespit edilen governance açığı kapatıldı** (reason yoksa satır hatası, audit'e yalnız ülke kodu). (6) **Apply stratejisi:** validate-all → apply-valid; kapılar: zorunlu referans seti yayınlanmamışsa **blok**, hata oranı **>%20 blok**, strict mod açıkken tek hata **blok**, uygulanacak satır yoksa blok; sıra Contacts→AccountLinks, bağımlı link satırı `skipped_dependency`; multi-document transaction **yok** (partial strateji UI+audit'te açık). (7) **Permission:** endpoint `crm.contact.import`; **satır seviyesinde** ek olarak `crm.contact.create`/`crm.contact.update`/`crm.account-contact.manage` aranır → import, UI yetkilerinin arka kapısı değil; eksikse satır `permission_denied` (fail-closed), **yeni permission icat edilmedi**. (8) **PII/KVKK:** önizleme etiketleri maskeli (`A** L****`), mesajlar kuralı anlatır değeri tekrarlamaz, audit **counts-only**, yüklenen dosya adı sabitlenir/loglanmaz, dosya diske yazılmaz. (9) **UI:** `/CRM/Contacts/Import` compact tam sayfa (offcanvas yok) — upload → **Validate** → özet kartları (create/update/end/skip/error/warning/conflict) + filtrelenebilir satır tablosu → **Apply** (MOD-0013 premium onay, "sonlandırılan bağlantılar silinmez" uyarısı); Import öğesi **ComingSoon değil**, permission yoksa hiç render edilmez; Gateway-only proxy, direct 5061 yok. **Gates:** CrmService **tests 170/170** (+41 `ContactWorkbookImportTests`), CrmApi/Web/Gateway build 0 hata, **compact verifier PASS/0 FAIL**, RESX **ContactIndex 121×7**, canlı smoke: upload route'ları **401**, import sayfası **302→login**, preview GET **404** (POST-only), FU06 import **405**, CSV export/template **401**; 5061 OpenAPI'sinde `import-file`/`import-file/apply`/`dryRun`/`strictMode` görünür. Boundary temiz (AccountRelationship XLSX yok, Territory/Visit/Patient/Consent yok, hard delete yok, `Operation=delete` desteklenmez, local seed/hardcoded fallback yok). Open: kimlik-doğrulamalı yükleme smoke'u (Low, kod+unit+route proof), indirilebilir XLSX **result report** (Medium), idempotency token/staging (Medium), `RowVersion` eşzamanlılık (Medium), Instructions sayfası İngilizce (Medium), CSV import yok (Low, bilinçli), Patient/Healthcare Privacy Scope (**High**, açık). **Next: MOD-0151 Territory Management Pack Prep.** W-3. |
| MOD-0151 Territory Management | Başlanmadı | 0 | — | Module pack required (W-4). Data scope needs MOD-0018-FU15. |
| MOD-0152 Lead Management | Başlanmadı | 0 | — | Module pack required (W-3, greenfield). |
| MOD-0153 Opportunity & Pipeline Management | Başlanmadı | 0 | — | Module pack required (W-3, greenfield). |
| MOD-0154 Forecasting & Quotas | Başlanmadı | 0 | — | Module pack required (W-4). |
| MOD-0155 Field Sales / Visit Planning | Başlanmadı | 0 | — | **Legacy preservation design pack recommended early; implementation follows Blueprint W-4.** |
| MOD-0156 Price Lists & Discount Guardrails | Başlanmadı | 0 | — | Module pack required (W-3). `commercial.*` namespace pending. |
| MOD-0157 Quote Generation | Başlanmadı | 0 | — | Module pack required (W-3). |
| MOD-0158 Quote-to-Contract Handoff | Başlanmadı | 0 | — | Module pack required (W-3). |
| MOD-0159 Product Configuration | Başlanmadı | 0 | — | Module pack required (W-4). Product SoR = MDM. |
| MOD-0160 Case Management | Başlanmadı | 0 | — | Module pack required (W-1). |
| MOD-0161 SLA Routing & Escalation | Başlanmadı | 0 | — | Module pack required (W-2). |
| MOD-0162 Knowledge Base | Devam ediyor | 60 | FU02 runtime (KnowledgeContent + Subject/Topic/AudienceProfile CRUD-minus-delete + archive + effective dating + contract + content-linkage read provider; CrmService, 23 test PASS) + CRM Admin → Knowledge UI (Content Compact + Taxonomy + nav + 7-dil RESX; Diten.Web build-green) — **authenticated Gateway smoke ALL PASS 22/0 (2026-08-10), FU02 done**. Boundary FU01 approved; FU01A/01C approved; FU01B held. | FU01A/01B/01C runtime (KnowledgePath/EngagementJourney/ConceptGraph); RBAC seed/grant (FU02-RBAC); MOD-0048 reference-set publish; F-L10N pro translation; parent Service/self-service KB channel. |
| MOD-0163 Customer Satisfaction Loop | Başlanmadı | 0 | — | Module pack required (W-4). |
| MOD-0164 Consent & Preference Management | Başlanmadı | 0 | — | Module pack required (W-2, early lane). |
| MOD-0165 Campaign Management | Başlanmadı | 0 | — | Module pack required (W-4). Segment → MOD-0167. |
| MOD-0166 Journeys & Automation | Başlanmadı | 0 | — | Module pack required (W-4). |
| MOD-0167 Segmentation / CDP | Başlanmadı | 0 | — | Module pack required (W-4). |
| MOD-0168 Order Capture | Başlanmadı | 0 | — | Module pack required (W-2). SoR (Finance) EA-TBD. |
| MOD-0169 Billing & Invoicing | Başlanmadı | 0 | — | Module pack required (W-2). Fulfils Blueprint MOD-0169; SoR EA-TBD. |
| MOD-0170 Returns (RMA) | Başlanmadı | 0 | — | Module pack required (W-3). SoR EA-TBD. |
| MOD-0171 Disputes / Claims | Başlanmadı | 0 | — | Module pack required (W-3). SoR EA-TBD. |
| MOD-0172 Allocation & ATP/CTP | Başlanmadı | 0 | — | Module pack required (W-4). SoR EA-TBD. |
| MOD-0282 Partner & Alliance Management | Başlanmadı | 0 | — | Module pack required (W-4). |
| MOD-0283 Pursuit & Proposal Management (RFP/RFI) | Başlanmadı | 0 | — | Module pack required (W-4). |
| MOD-0284 Deal Desk & Commercial Approvals | Başlanmadı | 0 | — | Module pack required (W-4). |

---

*Maintenance: update a module's row here whenever its implementation state changes (new frontend, tests added, feature completed). This file — not the identity registry — is the home for status. See the registry's link to this file.*
