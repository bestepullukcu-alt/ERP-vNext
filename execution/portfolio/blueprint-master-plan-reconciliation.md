# Blueprint-Master Plan Reconciliation

## Purpose
This document acts as the reconciliation tracking file between the high-level business capabilities (Enterprise Blueprint) and the technical module development plan. It ensures mapping alignment and guards against missing dependencies, Source of Record (SoR) collisions, page coverage issues, and release scope drift.

## Scope
Maintains mapping records, coverage status, and reconciliation governance rules between `execution/portfolio/enterprise-blueprint.md` and `execution/portfolio/master-development-plan.md`.

## Not the source for
- Writing code or module packages (use Module Packs).
- Individual module specifications or schemas (use docs/modules/ or Module Packs).

## Current status
Active. The Blueprint↔Repo canonicalization ledger and the unresolved EA reservation ledger (below) are established per **DCP-002 — Module Identity Canonicalization**. Blueprint (`docs/System Capability & Implementation Blueprint - master 5.xlsx`, `Blueprint_Data`) is the canonical MOD-ID + name authority. Canonical IDs not yet allocated by the Enterprise Architect remain **unresolved** (no placeholder IDs are assigned).

## Blueprint ↔ Repo Canonicalization Ledger (DCP-002, applied)

Documentation-only canonical alignment already applied to the working tree. Old IDs are deprecated aliases in [module-id-registry.md](../registries/module-id-registry.md) (chain resolves).

| Prior Repo ID | Prior Name | Canonical ID | Canonical Name | Type | Runtime literal touched? |
|---|---|---|---|---|---|
| MOD-0040 | Tenant Organization Foundation | **MOD-0288** | Organization, Person & Position Directory | rename (Blueprint canonical) | No (none in code) |
| MOD-0040-FU01 | Position Assignment User Reference Validation | **MOD-0288-FU01** | Position Assignment User Reference Validation | rename (child) | No |
| MOD-0043 | Tenant Architecture Foundation | **MOD-0009-FU01** | Tenant Architecture Foundation | tenant decomposition (child of MOD-0009) | No |
| MOD-0044 | Tenant Manager Backend | **MOD-0009-FU02** | Tenant Manager Backend | tenant decomposition | No |
| MOD-0046 | Tenant Core UI | **MOD-0009-FU03** | Tenant Core UI | tenant decomposition | No |
| MOD-0045 | Tenant Mgmt Legacy / Gap Reference | — (retired) | — | retire-legacy (non-executable) | No |
| MOD-0013 | Premium Modal Standard | — (standard) | (≈ Blueprint MOD-0286 UI Design System & Page Pattern Governance) | convert-to-standard; removed from module namespace | No |
| name-drift set (MOD-0018/0021/0023/0026/0027/0028/0032/0033/0034/0035/0038/0039/0041/0042/0262/0263/0265/0287) | various | same IDs | Blueprint canonical names | retain-id, name-align only | No (correct runtime literals unchanged) |

## Unresolved EA Reservation Ledger (pending explicit MOD-ID allocation)

> These records are **not** renamed in this pass. No placeholder MOD IDs are assigned. They are **not** converted to deprecated aliases until an exact replacement ID exists. No runtime literal is touched. State = pending explicit Enterprise Architect reservation/decision.

| Current ID | Current Meaning | Requested Canonical Capability | Existing Blueprint Candidate | Exact New ID Assigned? | Runtime Sensitive? | Next Decision Owner | Notes |
|---|---|---|---|---|---|---|---|
| MOD-0047 | Tenant User Foundation (AuthService read-only lookup-validation) | Tenant User / Identity Foundation primitive | none clean (≠ MOD-0288 org dir, ≠ MOD-0261 ext IdP, ≠ MOD-0018 authz) | **No — pending EA** | No (literals already correct) | Enterprise Architect | Squats Blueprint MOD-0047 (Business Continuity). Keep MOD-0047 active until exact new ID allocated; do not rename now. |
| PSS-004 | Tenant Login Security Settings | Tenant login/MFA security settings | none (platform-ops; not in Blueprint) | **No — pending EA** | No | EA | repo-only legacy prefix. |
| PSS-005 | Tenant Module Catalog | Tenant module catalog | partial: Blueprint MOD-0008 "Enterprise Capability / Product Catalog" (different scope) | **No — pending EA** | No | EA | MOD-0008 is a capability label within PSS-005 (see registry/DCP-002). |
| PSS-006 | Tenant Subscription Plan Catalog | Subscription plan catalog | none direct | **No — pending EA** | No | EA | repo-only. |
| PSS-007 | Platform Subscription Feature Management | Feature catalog / plan-feature mapping | none direct | **No — pending EA** | No | EA | repo-only. |
| PSS-008 | Module Details Assignment Inspection | Read-only assignment inspection | none | **No — pending EA** | No | EA | repo-only. |
| PSS-009 | Platform Admin Profile & Settings | Platform admin profile/settings | none | **No — pending EA** | No | EA | repo-only. |
| PSS-010 | Platform Admin Password & MFA Security | Platform admin security | none | **No — pending EA** | No | EA | repo-only. |
| PSS-011 | Lookups / Reference Data | Platform reference data / lookups | none direct | **No — pending EA** | No | EA | repo-only. |
| NEW-002 | Platform Administrators Management | Platform admin lifecycle | none | **No — pending EA** | No | EA | repo-only legacy prefix. |
| NEW-003 | Notification Template Management UI | Notification template CRUD/UI | possibly under MOD-0027 Notification Service | **No — pending EA** | No | EA | EA-003: standalone vs MOD-0027-FU; no active pack. |
| NEW-004 | Tenant Impersonation Tooling | Support/admin impersonation | none | **No — pending EA** | No | EA | EA-004: scope + security model undefined; no active pack. |
| MOD-0266 | Blob / File Storage Provider | Cloud/blob storage abstraction | Blueprint MOD-0266 "Cloud Infrastructure (AWS/Azure/GCP) [External Provider]" (same ID; likely same concept) | **No — pending EA confirm** | No | EA | Likely retain ID + name-align; confirm scope before applying. |
| MOD-0297 | Tenant Subscription Management | SaaS subscription lifecycle | none (not in Blueprint) | **No — pending EA** | **Yes** (Hangfire `TrialExpiryScanJob`, `SubscriptionRenewalJob`) | EA | repo-only; do NOT rename job literals. |
| MOD-0298 | Tenant Module Entitlements | Tenant module entitlements | none (not in Blueprint) | **No — pending EA** | No | EA | repo-only. |
| MOD-0299 ↔ MOD-0169 | SaaS Billing & Invoicing (repo MOD-0299) | SaaS billing capability | Blueprint MOD-0169 "Billing & Invoicing" (ERP / O2C — different domain) | **No — pending EA** | **Yes** (Hangfire `SubscriptionRenewalJob` owner literal `"MOD-0297/MOD-0299"`) | EA | EA-001 / EA-005: disambiguate SaaS vs ERP billing; do NOT rename job owner literal. |
| MOD-0008 | Module Catalog Assignable Expose | Assignable-expose contract within module catalog | Blueprint MOD-0008 "Enterprise Capability / Product Catalog" | **No — pending EA** | No | EA | Recommended: consolidate as capability label within PSS-005; confirm before aliasing. |
| MOD-0169 | Platform Reference (repo; vague) | (clarify) | Blueprint MOD-0169 "Billing & Invoicing" (ERP) | **No — pending EA** | No | EA | EA-005: clarify repo MOD-0169 vs Blueprint ERP billing vs MOD-0299 SaaS billing. |

## CAND-CAP Resolution (second pass — applied)

The unresolved items above have been mapped: `PSS-011 → MOD-0048`, `PSS-004 → MOD-0017-FU01`, `NEW-003 → MOD-0027-FU02`, `PSS-XCUT-SV → MOD-0287`; `MOD-0266`/`MOD-0008` name-aligned to Blueprint; `MOD-0169` retired; and the remainder to the temporary candidate namespace — `CAND-CAP-0001` (Tenant User / Identity Foundation ← MOD-0047), `CAND-CAP-0002` (SaaS Subscription, Plan & Entitlement ← MOD-0297, PSS-005/006/007/008, MOD-0298), `CAND-CAP-0003` (Platform Administration & Operations ← NEW-002, PSS-009/010), `CAND-CAP-0004` (Tenant Impersonation / Support Tooling ← NEW-004), `CAND-CAP-0005` (SaaS Billing & Invoicing ← MOD-0299). Candidate IDs are temporary governance identities pending Enterprise Architect canonical `MOD-xxxx` allocation; lifecycle: legacy → CAND-CAP alias → EA MOD-xxxx. Runtime literals `MOD-0297` / `MOD-0299` remain unchanged as legacy compatibility literals.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0006` is reserved as the temporary governance identity for `HR Capability Block Shell` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0297`, but `MOD-0297` remains the deprecated platform alias for Tenant Subscription Management / `CAND-CAP-0002` and must not be remapped to HR/HCM. `CAND-CAP-0006` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0007` is reserved as the temporary governance identity for `Employee Profile & Employment Record Projection` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0298`, but `MOD-0298` remains the deprecated platform alias for Tenant Module Entitlements / `CAND-CAP-0002-FU05` and must not be remapped to HR/HCM. `CAND-CAP-0007` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0008` is reserved as the temporary governance identity for `HR Governance & Sensitive Access Controls` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0314`, but `MOD-0314` is not present in the Blueprint and has no canonical registry row. `MOD-0018` remains the PSS RBAC / ABAC Authorization dependency/substrate and must not be reused as this HCM-native capability identity. `CAND-CAP-0008` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0009` is reserved as the temporary governance identity for `Position & Organization Assignment` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0299`, but `MOD-0299` remains the deprecated platform alias for SaaS Billing & Invoicing / `CAND-CAP-0005` and must not be remapped to HR/HCM. `MOD-0288`, `MOD-0288-FU01`, and `MOD-0288-FU02` remain PSS Organization, Person & Position Directory/reference substrate only and must not be reused as this HCM-native assignment identity. `CAND-CAP-0009` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0010` is reserved as the temporary governance identity for `Offboarding & Exit Management` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0305`, but `MOD-0305` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HR/HCM use. `CAND-CAP-0007`, `CAND-CAP-0008`, and `CAND-CAP-0009` remain HCM foundation dependencies only and must not be reused as this offboarding identity. Offboarding may plan a TEP handoff payload, but this reservation does not authorize TEP runtime, service, or module implementation. `CAND-CAP-0010` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0011` is reserved as the temporary governance identity for `Talent Ecosystem Platform Shell` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0321`, but `MOD-0321` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. HCM R1 foundation contracts may be consumed as prerequisites, but this reservation does not authorize TEP service scaffold, runtime implementation, frontend, gateway, or module code. `CAND-CAP-0011` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0012` is reserved as the temporary governance identity for `Association Membership & Member Company Registry` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0323`, but `MOD-0323` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011` TEP Shell remains a completed dependency, and HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain prerequisite contracts only. Privacy/legal/consent runtime decisions remain blockers for runtime-ready promotion. `CAND-CAP-0012` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0013` is reserved as the temporary governance identity for `Consent, Visibility & Access Policy` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0325`, but `MOD-0325` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. This candidate is reserved to define the policy boundary needed before `CAND-CAP-0012` Association runtime can become ready-for-dev. `MOD-0018` remains the PSS RBAC / ABAC Authorization dependency/substrate and must not be reused as this TEP-native consent/visibility identity. `CAND-CAP-0013` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0014` is reserved as the temporary governance identity for `Verified HR Participant & Company Access` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0324`, but `MOD-0324` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, and `CAND-CAP-0013` remain completed TEP dependencies, while HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain prerequisite context only. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, or HCM/PSS ownership transfer. `CAND-CAP-0014` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0015` is reserved as the temporary governance identity for `Talent Ecosystem Governance & Review Board` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0326`, but `MOD-0326` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, and `CAND-CAP-0014` remain completed TEP dependencies, but they do not authorize review-board runtime by themselves. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, dispute workflow, external review-board integration, or HCM/PSS ownership transfer. `CAND-CAP-0015` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0016` is reserved as the temporary governance identity for `Trust Level & Multi-Signature Engine` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0330`, but `MOD-0330` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `MOD-0022` e-Signature Service remains the PSS e-signature substrate/dependency and must not be reused as this TEP-native trust/multi-signature identity. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, and `CAND-CAP-0015` remain completed TEP dependencies, but they do not authorize trust/multi-signature runtime by themselves. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, signing workflow, signature execution, cryptographic service ownership, or HCM/PSS ownership transfer. `CAND-CAP-0016` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0017` is reserved as the temporary governance identity for `Industry Candidate Identity & Talent Profile` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0322`, but `MOD-0322` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, and `CAND-CAP-0016` remain completed TEP dependencies, but they do not authorize candidate/talent identity runtime by themselves. Candidate/talent identity privacy, legal, consent, data-scope, and data-minimization runtime decisions remain blockers for runtime-ready promotion. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, candidate profile ownership, talent profile runtime, raw/sensitive payload persistence, or HCM/PSS ownership transfer. `CAND-CAP-0017` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0018` is reserved as the temporary governance identity for `Industry Exit Reference Record Registry` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0327`, but `MOD-0327` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, and `CAND-CAP-0017` remain completed TEP dependencies, while HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain prerequisite context only. PSS `MOD-0288` and completed external provider metadata screens remain dependency/backbone context only and are not reused as this TEP-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, reference exchange marketplace, rehire recommendation, dispute workflow runtime, raw provider payload persistence, credential/token/secret persistence, PII-heavy persistence, or HCM/PSS ownership transfer. `CAND-CAP-0018` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0019` is reserved as the temporary governance identity for `Reference Exchange Marketplace` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0328`, but `MOD-0328` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, and `CAND-CAP-0018` remain completed TEP dependencies, while HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain prerequisite context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this TEP-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, cross-company reference exchange runtime, rehire recommendation, candidate dispute/response workflow, raw provider payload persistence, credential/token/secret persistence, PII-heavy persistence, or HCM/PSS ownership transfer. `CAND-CAP-0019` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0020` is reserved as the temporary governance identity for `Rehire Recommendation Network` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0329`, but `MOD-0329` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, `CAND-CAP-0018`, and `CAND-CAP-0019` remain completed TEP dependencies, while HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain prerequisite context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this TEP-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, rehire recommendation runtime, scoring/ranking/analytics or automated decision behavior, candidate dispute/response workflow, raw provider payload persistence, credential/token/secret persistence, PII-heavy persistence, or HCM/PSS ownership transfer. `CAND-CAP-0020` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**TEP candidate reservation (EA/registry owner decision).** `CAND-CAP-0021` is reserved as the temporary governance identity for `Candidate Response & Dispute Management` in `talent-ecosystem-platform`. The Excel/DCP-008 row proposed `MOD-0331`, but `MOD-0331` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for TEP use. `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, `CAND-CAP-0018`, `CAND-CAP-0019`, and `CAND-CAP-0020` remain completed TEP dependencies, while HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain prerequisite context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this TEP-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, candidate-facing dispute/response workflow runtime, notification/document integration, controlled/external document repository integration, candidate self-service UX, raw provider payload persistence, credential/token/secret persistence, PII-heavy persistence, or HCM/PSS ownership transfer. Legal/privacy/consent/retention/evidence decisions remain blockers before runtime-ready promotion. `CAND-CAP-0021` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0022` is reserved as the temporary governance identity for `Recruitment / Applicant Intake` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0300`, but `MOD-0300` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, and `CAND-CAP-0010` remain completed dependencies. TEP `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, `CAND-CAP-0018`, `CAND-CAP-0019`, `CAND-CAP-0020`, and `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, candidate-facing public application UX, resume/CV body persistence, cover letter persistence, free-text application narrative persistence, attachment payload persistence, notification/document integration, raw provider payload persistence, credential/token/secret persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0022` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0023` is reserved as the temporary governance identity for `Candidate Pipeline & Interview Management` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0301`, but `MOD-0301` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, and `CAND-CAP-0022` remain completed dependencies. TEP `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, `CAND-CAP-0018`, `CAND-CAP-0019`, `CAND-CAP-0020`, and `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, candidate-facing scheduling/application UX, interview note persistence, free-text evaluation persistence, resume/CV body persistence, attachment payload persistence, notification/document integration, scoring/ranking/automated decision behavior, raw provider payload persistence, credential/token/secret persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0023` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0024` is reserved as the temporary governance identity for `Offer Management` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0302`, but `MOD-0302` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, `CAND-CAP-0022`, and `CAND-CAP-0023` remain completed dependencies. TEP `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, `CAND-CAP-0018`, `CAND-CAP-0019`, `CAND-CAP-0020`, and `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, offer letter document generation, candidate-facing offer acceptance UX, notification/document integration, compensation/benefits/payroll payload persistence, raw provider payload persistence, credential/token/secret/password persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0024` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0025` is reserved as the temporary governance identity for `Employee Onboarding` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0303`, but `MOD-0303` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, `CAND-CAP-0022`, `CAND-CAP-0023`, and `CAND-CAP-0024` remain completed dependencies. TEP `CAND-CAP-0011`, `CAND-CAP-0012`, `CAND-CAP-0013`, `CAND-CAP-0014`, `CAND-CAP-0015`, `CAND-CAP-0016`, `CAND-CAP-0017`, `CAND-CAP-0018`, `CAND-CAP-0019`, `CAND-CAP-0020`, and `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, employee onboarding workflow runtime, candidate/employee-facing onboarding UX, notification/document integration, controlled/external document repository integration, banking/payroll/tax persistence, raw provider payload persistence, attachment payload persistence, free-text narrative persistence, credential/token/secret/password persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0025` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0026` is reserved as the temporary governance identity for `Employment Change / Transfer / Promotion` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0304`, but `MOD-0304` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, `CAND-CAP-0022`, `CAND-CAP-0023`, `CAND-CAP-0024`, and `CAND-CAP-0025` remain completed dependencies. TEP `CAND-CAP-0011` through `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, employment change workflow runtime, transfer/promotion approval workflow, compensation/benefits/payroll payload persistence, position assignment mutation, employee-facing or manager-facing action UX, notification/document integration, controlled/external document repository integration, raw provider payload persistence, attachment payload persistence, free-text narrative persistence, credential/token/secret/password persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0026` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0027` is reserved as the temporary governance identity for `Performance Review Management` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0306`, but `MOD-0306` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, `CAND-CAP-0022`, `CAND-CAP-0023`, `CAND-CAP-0024`, `CAND-CAP-0025`, and `CAND-CAP-0026` remain completed dependencies. TEP `CAND-CAP-0011` through `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, performance review cycle/runtime workflow, goal scoring, rating, calibration, ranking behavior, manager/employee review UX, free-text review notes, appraisal narrative persistence, attachment payload persistence, notification/document integration, raw provider payload persistence, credential/token/secret/password persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0027` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0028` is reserved as the temporary governance identity for `Competency & Skills Assessment` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0307`, but `MOD-0307` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, `CAND-CAP-0022`, `CAND-CAP-0023`, `CAND-CAP-0024`, `CAND-CAP-0025`, `CAND-CAP-0026`, and `CAND-CAP-0027` remain completed dependencies. TEP `CAND-CAP-0011` through `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, competency/skills assessment workflow, skill scoring, rating, ranking, calibration, automated decision behavior, manager/employee assessment UX, free-text assessment notes, appraisal narrative persistence, attachment payload persistence, notification/document integration, raw provider payload persistence, credential/token/secret/password persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0028` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

**HCM candidate reservation (EA/registry owner decision).** `CAND-CAP-0029` is reserved as the temporary governance identity for `Development Plan Management` in `human-capital-management`. The Excel/DCP-008 row proposed `MOD-0308`, but `MOD-0308` is not present in the Blueprint and has no canonical registry row, so DCP-002 blocks it for HCM use. HCM foundation `CAND-CAP-0007`, `CAND-CAP-0008`, `CAND-CAP-0009`, `CAND-CAP-0010`, `CAND-CAP-0022`, `CAND-CAP-0023`, `CAND-CAP-0024`, `CAND-CAP-0025`, `CAND-CAP-0026`, `CAND-CAP-0027`, and `CAND-CAP-0028` remain completed dependencies. TEP `CAND-CAP-0011` through `CAND-CAP-0021` remain dependency/context only. PSS `MOD-0288`, `MOD-0251`, `MOD-0279`, `MOD-0280`, and `MOD-0281` remain dependency/backbone context only and are not reused as this HCM-native identity. This reservation does not authorize runtime implementation, service scaffold, frontend, gateway, development plan workflow, goal/learning assignment execution, coaching/manager action UX, skill gap scoring, rating, ranking, recommendation or automated decision behavior, free-text development notes, coaching narrative persistence, attachment payload persistence, notification/document integration, raw provider payload persistence, credential/token/secret/password persistence, PII-heavy persistence, or TEP/PSS ownership transfer. `CAND-CAP-0029` is not Blueprint-backed, is not a runtime literal, and is pending future EA canonical `MOD-xxxx` allocation. Candidate gate not executable in this checkout; reservation recorded.

## Minimal SoR Reconciliation Records

| Field | Value |
|---|---|
| Business capability | Legal Entity Management |
| Canonical system-of-record | MDM Legal Entity capability |
| Reserved module ID | `MOD-0220` confirmed |
| Reservation basis | Explicit user decision after authoritative planning Excel mapping confirmation |
| Schema review | MOD-0220 minimal backend schema reconciliation reviewed and approved; Legal Entity Foundation is ready-for-dev for the narrow backend-only slice |
| Remaining reconciliation | Authoritative Enterprise Blueprint repository migration pending as non-blocking follow-up |
| Owner domain | MDM |
| Consumer module | `MOD-0288 Organization, Person & Position Directory` (canonicalized from MOD-0040 per DCP-002) |
| Consumer relationship | Read-only `LegalEntityId` reference / lookup validation dependency; validation contract locked and unchanged |
| Forbidden duplication | No Legal Entity aggregate, persistence, lifecycle, API or UI under MOD-0288 |
| Decision gate | `OD-MOD-le-contract` resolved; MOD-0288 pack ready-for-dev for the minimal backend-only v1 slice |
| Related follow-up | Complete authoritative Enterprise Blueprint repository migration; define MDM business-country reference source distinct from PSS-011 |

## Boundary Notes

- PSS-011 countries lookup is Platform provisioning/support only.
- It is not the MDM business-country system of record.
- MDM business-country reference ownership remains a separate follow-up.

## Source / migration note
New target file designed for governance alignment between the business capability matrix (Excel blueprint) and technical implementation modules.

**Workbook supersession (user decision).** The prior planning workbook `execution/modules_pages_planning_v3.xlsx` is intentionally retired and removed from the repository by explicit user decision; `docs/System Capability & Implementation Blueprint - master 5.xlsx` is the authoritative canonical enterprise module-ID source going forward. No claim is made that every historical sheet was migrated one-to-one.

## Owner / update rule
- Owner: Enterprise Architect / PMO
- Update Rule: Updated during wave planning alignment sessions and before release gate reviews. New canonical MOD-ID allocations for unresolved items above require explicit Enterprise Architect reservation recorded in [module-id-registry.md](../registries/module-id-registry.md) before any rename.
