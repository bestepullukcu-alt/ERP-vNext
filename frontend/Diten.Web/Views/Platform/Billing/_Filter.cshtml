@using Diten.Web.Views.Platform.Billing
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<BillingIndex> Localizer
@inject IHtmlLocalizer<Diten.Web.SharedResource> SharedLocalizer

<div id="inlineFilterHost">
    <div class="collapse" id="inlineFilterCollapse">
        <div class="pt-0 pb-3">
            <form class="m-0" id="filterForm" data-no-tracker>
                <div class="dt-filter-bar d-flex flex-wrap align-items-center gap-3">
                    <div class="filter-chip filter-status">
                        <select id="filterStatus" class="form-select form-select-sm select2" multiple="multiple" data-placeholder="@SharedLocalizer["Status"]">
                            <option value="Draft">@Localizer["StatusDraft"]</option>
                            <option value="Issued">@Localizer["StatusIssued"]</option>
                            <option value="PartiallyPaid">@Localizer["StatusPartiallyPaid"]</option>
                            <option value="Paid">@Localizer["StatusPaid"]</option>
                            <option value="Cancelled">@Localizer["StatusCancelled"]</option>
                        </select>
                    </div>

                    <div class="filter-chip filter-currency">
                        <select id="filterCurrency" class="form-select form-select-sm select2" multiple="multiple" data-placeholder="@Localizer["Currency"]">
                        </select>
                    </div>

                    <div class="ms-auto d-flex gap-3">
                        <button type="button" id="btnFilterApply" class="btn btn-sm btn-primary">@SharedLocalizer["Apply"]</button>
                        <button type="reset" id="btnFilterReset" class="btn btn-sm btn-label-danger">@SharedLocalizer["Reset"]</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
