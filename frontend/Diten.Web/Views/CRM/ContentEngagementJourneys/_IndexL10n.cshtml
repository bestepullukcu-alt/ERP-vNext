@using System.Text.Json
@using Diten.Web.Views.CRM.ContentEngagementJourneys
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<ContentEngagementJourneysIndex> Localizer
@{
    var keys = new[]
    {
        "Actions", "Active", "Passive", "Unknown", "Edit", "ViewDetails", "QuickView",
        "BulkDelete", "BulkDeleteConfirm", "AreYouSure", "Cancel", "Save", "No", "Yes",
        "Search", "Export", "Import", "Filter", "Apply", "Reset", "ShowAll", "SaveView", "ColumnVisibility", "Status",
        "ContentEngagementJourneyContractUnavailable", "JourneyCode", "JourneyName", "JourneyVersion", "JourneyStatus",
        "SubjectId", "LanguageCode", "ActiveStageCount", "RequiredStageCount", "PathResolution", "EffectiveFrom",
        "EffectiveTo", "Archived", "UpdatedAt", "CreateJourney", "EditJourney", "ArchiveJourney",
        "ArchiveJourneyConfirm", "EmptyState", "ErrorState", "Loading", "RecordArchived", "IncludeArchived",
        "Unresolved", "Resolved", "Frozen", "Repeated", "SelectOption"
    };
    var values = keys.ToDictionary(x => x, x => Localizer[x].Value);
    // Verifier-compatible property markers for the dictionary-backed JSON bridge (each token below is `Key =`):
    // Active =, Passive =, Unknown =, Actions =, Edit =, ViewDetails =, QuickView =,
    // BulkDelete =, BulkDeleteConfirm =, AreYouSure =, Cancel =, Search =, Export =,
    // Import =, Filter =, Apply =, Reset =, ShowAll =, SaveView =, ColumnVisibility =, Status =
}
<script id="content-engagement-journeys-l10n" type="application/json">@Html.Raw(JsonSerializer.Serialize(values))</script>
