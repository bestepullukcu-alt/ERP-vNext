<div id="workCenterPage" data-initial-tab="@(ViewBag.InitialTab ?? "inbox")">
    <partial name="WorkCenterHeader" />
    <partial name="WorkCenterTabs" />

    <div class="tab-content p-0">
        <partial name="InboxTab" />
        <partial name="AllWorkTab" />
    </div>

    <partial name="InboxRow" />
</div>
