@model Diten.Web.Models.HumanCapital.CompensationBenefits.CompensationBenefitsEditViewModel
@using Diten.Web.Views.HumanCapital.CompensationBenefits
@using Microsoft.AspNetCore.Mvc.Localization
@inject IHtmlLocalizer<CompensationBenefitsIndex> Localizer
@{
    ViewData["Title"] = Localizer["FormTitleCreate"].Value;
    Layout = "_LayoutTenantShell";
}

<partial name="~/Views/HumanCapital/CompensationBenefits/_Form.cshtml" model="Model" />

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var select2Elements = $('.select2');
            if (select2Elements.length) {
                select2Elements.each(function () {
                    var $this = $(this);
                    $this.wrap('<div class="position-relative"></div>').select2({
                        dropdownParent: $this.parent(),
                        minimumResultsForSearch: Infinity
                    });
                });
            }
        });
    </script>
}
