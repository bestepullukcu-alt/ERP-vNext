# MOD-0151 — FU01 Live Smoke (Contract + TerritoryModel + TerritoryNode)

> **Tarih:** 2026-07-23 · **Tür:** Live smoke + closeout doğrulama (geliştirme değil)
> **Tenant:** `97c59330-dbc4-4665-b29c-0c26dbb5cc93` · **Scope:** tenant
> **Verdict:** **PARTIAL** — backend build/test PASS; canlı smoke **bloklu** (published-values 97c59330 için hazır değil + CRM token/RBAC yok)
> **Runtime/pack/gateway/UI/data:** DEĞİŞTİRİLMEDİ

---

## 1. Preflight

**Files reviewed:** [FU01 implementation report](./mod-0151-fu01-contract-territory-model-node-backend-2026-07-23.md) ·
[publish execution report](./mod-0151-territory-reference-publish-execution-2026-07-23.md) ·
[publish operator runbook](../../execution/domains/commercial-suite/reference-data/mod-0151-territory-reference-publish-operator-runbook.md) ·
[MOD-0151 pack](../../execution/domains/commercial-suite/module-packs/MOD-0151-territory-management.md) ·
[FU00 closeout](./mod-0151-fu00-pack-approval-closeout-2026-07-23.md) · F1 template · CrmService `TenantResolutionMiddleware`,
`TerritoryModelsController`/`TerritoryContractController`, gateway CRM routes.

**Tenant/scope confirmation:** Hedef tenant `97c59330-dbc4-4665-b29c-0c26dbb5cc93`, scope=tenant. **Kritik runtime gerçeği
(koddan):** CrmService tenant'ı **yalnızca `X-Tenant-Id` header'ından** çözer (`TenantResolutionMiddleware.cs:8,18`),
JWT claim'inden değil; header yoksa tenant context temizlenir → handler "Tenant context is required" (400). Smoke bu
yüzden `X-Tenant-Id: 97c59330…` göndermelidir. `TenantId` payload'dan **asla** gelmez.

**No-code-change confirmation:** Bu task hiçbir runtime kod, MOD-0151 kodu, MOD-0048 data, reference set/value publish,
registry, `ocelot.json`, UI, permission seed dosyasına dokunmadı. Yalnız (a) bu evidence raporu ve (b) operatörün
çalıştıracağı bir smoke script'i oluşturuldu.

**Published-values status:** **PENDING for 97c59330.** Publish execution konuşmasında set'ler yanlış tenant'a
(`00000000-0000-0000-0000-000000000001`) oluşturuldu ve yalnız **SUBMITTED** aşamasına geldi (approve+publish yok).
Doğru tenant (97c59330) için maker girişinin (`X-Tenant-Id` header'lı login) + approve/publish akışının tamamlandığına
dair **teyit yok**. Bu nedenle 97c59330 published-values **hazır kabul edilemez**.

---

## 2. Live Environment Probe (no auth — auth wall confirmation)

| Probe | Result | Notes |
|---|---|---|
| Gateway `GET /health` | **200** | ayakta |
| Platform `GET /health` | **200** | ayakta |
| CrmService `GET /health` | **200** | ayakta (port 5061) |
| `GET /api/crm/territory-management/contract` (anon) | **401** | auth zorunlu — token'sız okunamaz |
| `GET /api/crm/territory-models` (anon) | **401** | auth zorunlu |
| `GET /api/v1/reference-data/sets/territory-level/published-values?scope_key=97c59330…` (anon) | **401** | auth zorunlu → readiness token'sız doğrulanamaz |

Sonuç: Servisler sağlıklı, ancak tüm okuma/yazma çağrıları kimlik doğrulaması ister. Bu task bağlamında geçerli bir
CRM (97c59330) token'ı yoktur → canlı contract/model/node smoke **yürütülemedi**.

---

## 3. Backend Build / Test (code-level smoke — token gerektirmez)

| Suite | Result | Notes |
|---|---|---|
| CrmService Territory tests | **45/45 PASS** | contract/model/node/reference/permission/guard; izole output'a derlenip koşuldu (fleet'e dokunulmadı) |
| FU01 tam suite (önceki task) | 214/214 PASS | regresyon yok |

Backend mantığı (validation, fail-closed reference, hierarchy rank, microzone, tenant isolation) kod düzeyinde
doğrulanmış durumda. Eksik olan tek şey **canlı published-values + yetkili token**.

---

## 4. Published-values Smoke

| SetCode | Expected | Actual | Metadata Smoke | Result |
|---|---|---|---|---|
| territory-coverage-scope | 7 | — | — | **BLOCKED** (401 / not published for 97c59330) |
| territory-level | 6 | — | — | **BLOCKED** |
| territory-model-status | 6 | — | — | **BLOCKED** |
| territory-node-status | 4 | — | — | **BLOCKED** |
| territory-assignment-status | 4 | — | — | **BLOCKED** |
| territory-assignment-source | 4 | — | — | **BLOCKED** |
| business-scope-type | 7 | — | — | **BLOCKED** |
| territory-resource-role | 11 | — | — | **BLOCKED** |
| territory-rule-type | 9 | — | — | **BLOCKED** |
| territory-conflict-policy | 4 | — | — | **BLOCKED** |

Toplam beklenen 62 required value → **doğrulanamadı** (publish 97c59330 için tamamlanmadı, ayrıca token yok).

---

## 5. Contract Smoke

| Check | Expected | Actual | Result |
|---|---|---|---|
| HTTP 200 | 200 | 401 (anon) | **BLOCKED** — token yok |
| moduleId / runtimeScope / tenantId / isReady / flags / permissions | doğru | — | **BLOCKED** |

`isReady=true` ancak 10 required set 97c59330 scope'unda publish edilince beklenir; şu an publish tamamlanmadığı için
`isReady=false` (fail-closed) beklenir — bu doğru davranıştır, kod hatası değildir.

---

## 6. Positive & Negative Smoke

| Step | Endpoint | Result | Notes |
|---|---|---|---|
| TerritoryModel create/get/list/update | `/api/crm/territory-models` | **BLOCKED** | token + published `territory-model-status` gerekir |
| TerritoryNode root/child/microzone/list/update | `/api/crm/territory-models/{id}/nodes` | **BLOCKED** | token + published `territory-level`/`territory-node-status` gerekir |
| Negative (dup code, invalid date, dup territory code, backward rank, invalid level, microzone-on-zone, child-date-outside-parent) | — | **BLOCKED** | token gerekir |
| non-draft mutation | — | **SKIP (by design)** | FU01'de activation/status-transition endpoint'i yok; DB hand-edit yasak |
| cross-tenant access | — | **SKIP** | ikinci tenant token'ı yok; payload TenantId ile simüle **edilmez** |

> Bu senaryoların tümü, kullanıcı 97c59330 CRM token'ıyla `smoke-mod-0151-fu01-territory.ps1`'i çalıştırdığında
> otomatik koşulacak şekilde script'e kodlandı (pozitif + 7 negatif; non-draft & cross-tenant güvenli SKIP).

---

## 7. Cleanup Status

- **Records created (bu task):** 0 (canlı yazma yapılmadı — token yok).
- **Retained:** Önceki publish denemesinden `...0001` tenant'ında 12 SUBMITTED draft var (zararsız, publish edilmedi).
- **Reason:** FU01'de delete endpoint yok; smoke çalıştırıldığında `SMOKE-MOD0151-*` / `TR-*-SMOKE` kodlu kayıtlar
  **draft** olarak kalır (kod prefix'iyle sonradan bulunabilir). **Mongo hand-delete yapılmaz.**

---

## 8. Created / Updated Files

| File | Action | Notes |
|---|---|---|
| `docs/audits/mod-0151-fu01-live-smoke-2026-07-23.md` | Created | Bu evidence raporu |
| `execution/domains/commercial-suite/reference-data/smoke-mod-0151-fu01-territory.ps1` | Created | Operatörün 97c59330 CRM token'ıyla çalıştıracağı live smoke (contract + model + node + 7 negatif); read-only reference, yalnız SMOKE-* test kaydı |

Runtime kod / MOD-0151 / MOD-0048 data / gateway / UI / registry / pack: **dokunulmadı**.

---

## 9. Guard Checks

| Check | Result |
|---|---|
| Runtime code changed? | **no** |
| MOD-0151 code changed? | **no** |
| MOD-0048 data changed? | **no** |
| Reference set/value published? | **no** |
| Registry changed? | **no** |
| ocelot changed? | **no** |
| UI changed? | **no** |
| Permission seed changed? | **no** |
| Direct 5061 used? | **no** (yalnız /health probe; smoke Gateway-only) |
| TenantId correct? | **yes** (97c59330; header-based, payload değil) |
| Published-values ready? | **no** (97c59330 için publish tamamlanmadı) |
| Contract isReady true? | **unverified** (token yok; publish yok → beklenen false) |
| Feature flags out-of-scope false? | **verified in code/tests** (canlı doğrulama bloklu) |
| Model create success? | **blocked** |
| Node hierarchy create success? | **blocked** |
| Negative validations pass? | **verified in unit tests (45/45); live blocked** |
| Hardcoded fallback introduced? | **no** |
| Mongo hand-edit used? | **no** |
| Local seed used? | **no** |
| Assignment / Resource / Activation / Evidence endpoint called? | **no** (yok zaten) |
| Product/Brand master touched? | **no** |
| Account/Contact entity touched? | **no** |
| Smoke records cleanup status? | **n/a** (0 kayıt oluşturuldu; script çalışınca draft kalır) |

---

## 10. Final Verdict

**PARTIAL.**

Backend build/test **PASS** (45/45 Territory, 214/214 suite) ve servisler ayakta. Ancak canlı smoke **iki ön koşul
nedeniyle bloklu**: (1) **published-values 97c59330 için hazır değil** — publish yanlış tenant'a (`…0001`) gitti ve
SUBMITTED'da kaldı, doğru tenant için approve+publish tamamlanmadı; (2) bu task bağlamında **geçerli CRM (97c59330)
token'ı yok** ve `crm.territory.*` izinleri henüz bir role seed/atanmadı → yetkili token olsa bile
**SMOKE_BLOCKED_BY_RBAC_ASSIGNMENT** (403) beklenir. Hiçbir güvensiz workaround (Mongo hand-edit, payload TenantId,
publish-override, hardcoded fallback) uygulanmadı. Kullanıcının tek komutla koşacağı live smoke script'i hazır.

---

## 11. Next Recommended Prompt (PARTIAL follow-up)

Sırasıyla:

1. **MOD-0048 Territory publish — doğru tenant düzeltmesi:** 97c59330 maker (login'e `X-Tenant-Id: 97c59330…`
   header'ı ekleyerek) ile `publish-mod-0151-territory-reference.ps1 -MakerToken $maker -SendTenantHeader` çalıştır;
   ardından farklı bir checker ile approve+publish. Smoke: 10 set / 62 value, tenant 97c59330. (`…0001`'deki draft'lar
   zararsız kalır.)
2. **MOD-0151 RBAC role assignment:** tenant 97c59330'da bir role `crm.territory.read/.model.read/.model.manage/.node.read/.node.manage`
   ata (MOD-0048 governance dışı, ayrı RBAC adımı; Mongo hand-edit yok). Smoke token bu role sahip kullanıcıya ait olmalı.
3. **FU01 live smoke retry:** yukarıdaki ikisi tamamlanınca `smoke-mod-0151-fu01-territory.ps1 -Token $crm` çalıştır;
   contract `isReady=true` + model/node pozitif + 7 negatif PASS beklenir → bu rapor PASS'e güncellenir.
4. PASS sonrası: **MOD-0151 FU02 Territory Hierarchy UI / Territory Model Viewer** veya **FU03 Assignment Rules + Preview**.
