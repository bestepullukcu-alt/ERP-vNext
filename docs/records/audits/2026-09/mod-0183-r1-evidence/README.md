# MOD-0183 evidence index

This directory retains the original R1 run evidence plus the R2 rework evidence
under [r2/](r2/). Historical R1 logs are not overwritten.

## Protected-input audit

[baseline-preservation.json.txt](baseline-preservation.json.txt) contains the
**byte-identical original JSON payload**, including all 172 paths and SHA-256
hashes. DEV-02-R1 explicitly authorizes its audit-text representation to avoid
treating historical path evidence as executable code in DocsPathGuard.

Former filename: baseline-preservation.json.
SHA-256 before and after representation change:
66fa3dbdcd88e53c5aed9a6e071071033a641328d33cf24fe9d62e222ef7c6e1.
No path is obscured, encoded differently or discarded. Parse it as JSON.

The active [changed-files.json](changed-files.json) is the complete **current**
deliverable inventory. Its single self-reference omits a hash; every other entry
must have a valid SHA-256 and exist within the work package's allowed paths.

~~~sh
python3 services/Diten.SupplyChainService/tests/verify_evidence.py \
  . \
  docs/records/audits/2026-09/mod-0183-r1-evidence/baseline-preservation.json.txt \
  docs/records/audits/2026-09/mod-0183-r1-evidence/changed-files.json
~~~

The verifier checks all protected hashes, path boundaries, file existence,
non-self hashes, duplicate paths and the exact expected deliverable file set.

Current result and rework scope: [R3 report](../mod-0183-dev-r3-report-2026-09-16.md).
R1 report is retained as historical evidence with its migrated link and a
supersession note. Neither report constitutes independent CT acceptance.

## DEV-03 evidence-capture correction

The current r2/runtime/ files and r2/runtime.log were regenerated by DEV-03,
using a fresh isolated replica set. They supersede the six inaccurate request
snapshots from the R2 run. All 33 requests are compared against a separately
captured transport-boundary body in r2/runtime/actual-sent.json;
r2/runtime/sent-body-comparison.json must show zero mismatches.

The r3/ directory contains capture RED/GREEN tests, fresh build/service and
architecture results, and a six-record before/after summary. Original R1 evidence
and non-runtime historical R2 results remain historical. No verifier temporary
artifact was copied into this evidence set. Independent VER-03 is still required.

## DEV-04 inactive internal integration slice

[r4/](r4/) contains the separate Warehouse/Inventory mock-backed internal
coordinator run, source persistence and process-restart evidence. See the
[R4 report](../mod-0183-dev-r4-report-2026-09-16.md) for exact scope, current
inventory and known frozen-example incompatibility. R3 bounded core verification
remains historical evidence for that scope; R4 requires its own independent
verification. No automatic intake or live integration is claimed.
