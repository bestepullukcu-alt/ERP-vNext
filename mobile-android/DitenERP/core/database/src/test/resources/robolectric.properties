# compileSdk/targetSdk is 37, which Robolectric 4.14.1 has no runtime image for.
# Pin the unit-test SDK to the newest image this Robolectric ships (API 35);
# the cache logic under test is SDK-agnostic (minSdk 26).
sdk=35
