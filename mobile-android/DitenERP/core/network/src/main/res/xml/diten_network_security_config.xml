<?xml version="1.0" encoding="utf-8"?>
<!--
  Diten ERP mobile - network security policy.

  Cleartext HTTP is globally forbidden. It is allowed ONLY for the development
  gateway reached from the Android emulator (10.0.2.2 = host loopback) and from
  localhost, which is where the dev BuildConfig.BASE_URL points. Production /
  release traffic targets an HTTPS origin and therefore never triggers a
  cleartext exception. This is the tightest feasible scoping: per-domain opt-in
  rather than a blanket application-level usesCleartextTraffic="true".
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
